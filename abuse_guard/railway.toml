[build]
builder = "DOCKERFILE"
dockerfilePath = "abuse_guard/Dockerfile"

[deploy]
startCommand = "python -m abuse_guard"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
