## 1. Roadmap and Inspection

- [x] 1.1 Re-read the `### Subphase 4.11.6 — Resolve Remaining Ruff C401 Findings [ ]` placeholder entry in `openspec/specs/project.md` to confirm the documented scope: only `backend/intents/context/order_line_selection_resolver.py` and `backend/intents/context/product_modification_resolver.py` are touched, only the three `sorted(set(int(...) for ...) & set(int(...) for ...))` intersection sites are rewritten, and no other file is changed.
- [x] 1.2 Re-run `PYTHONPATH=. venv/bin/python -m ruff check backend --select C401 --output-format=concise` from the repo root and confirm exactly six findings at the expected locations: `backend/intents/context/order_line_selection_resolver.py:125`, `:126`, `backend/intents/context/product_modification_resolver.py:160`, `:161`, `:220`, `:221`. Confirm no other source file in `backend/` carries a `C401` finding. (Commands in this change are executed from the repo root so the `venv/bin/` interpreter and the `backend/...` paths resolve to their real locations; do not prepend `cd backend &&` to any command below — that would double the `backend/` prefix and break path resolution.)
- [x] 1.3 Read `backend/intents/context/order_line_selection_resolver.py` lines 115-135 and confirm the intersection at lines 124-127 (`recognized_ids` ∩ `active_intent.candidate_ids`) is the only site in the file. Confirm the surrounding `sorted(...)` wrapper, the `if not intersection` early-return, the `len(intersection) == 1` branch, the multi-candidate fallback, and the `diagnostic_sink.on_resolver_completed(...)` call are preserved verbatim.
- [x] 1.4 Read `backend/intents/context/product_modification_resolver.py` lines 155-225 and confirm the two intersections at lines 159-162 (`recognized_pp_ids` ∩ `source_candidate_ids`, inside `_resolve_source_selection`) and lines 219-222 (`recognized_dest_ids` ∩ `destination_candidate_ids`, inside `_resolve_destination_selection`) are the only C401 sites in the file. Confirm the `_build_ready_intent` / `_build_pending_intent` call sites, the `active_intent.model_copy(update={"status": "rejected"})` early-returns, and the surrounding `if len(destination_candidate_ids) == 1` / `if len(intersection) == 1` branches are preserved verbatim.
- [x] 1.5 Confirm the change does NOT touch `backend/recognizers/product_recognizer.py`, `backend/recognizers/product_recognizer_contract.py`, `backend/recognizers/fuzzy_product_recognizer.py`, `backend/services/product_recognition_calibration_runner.py`, `backend/services/product_recognition_calibration_commerce_catalog.py`, `backend/services/product_recognition_shadow_service.py`, `backend/intents/resolvers/*`, `backend/intents/orchestration/*`, `backend/intents/handlers/*`, `backend/intents/recognizers/*`, the FastAPI routers, the seed scripts, the dataset, the settings, the pyproject.toml, the ruff configuration, or any test file. The pre-flight `rg "set\(int\(" backend/intents/context/order_line_selection_resolver.py backend/intents/context/product_modification_resolver.py` returns exactly six matches (three pair) and `rg "set\(int\(" backend/` returns zero matches in any other resolver / recognizer / calibration file.

## 2. Rewrite the Order Line Selection Resolver Intersection

- [x] 2.1 In `backend/intents/context/order_line_selection_resolver.py`, replace the `set(int(cid) for cid in recognized_ids)` expression at line 125 with `{int(cid) for cid in recognized_ids}` (set comprehension). The replacement is expression-equivalent: both constructs produce a `set[int]` with the same membership and the same semantics under the `&` intersection that follows.
- [x] 2.2 In `backend/intents/context/order_line_selection_resolver.py`, replace the `set(int(cid) for cid in active_intent.candidate_ids)` expression at line 126 with `{int(cid) for cid in active_intent.candidate_ids}`. The replacement is expression-equivalent: both constructs produce a `set[int]` with the same membership and the same semantics under the `&` intersection that precedes them.
- [x] 2.3 Confirm the surrounding `sorted(...)` wrapper at lines 124-127, the `if not intersection: return active_intent.model_copy(update={"status": "rejected"})` early-return at line 129-130, the `if len(intersection) == 1: return _build_ready_intent(active_intent, intersection[0])` branch at lines 132-133, and the multi-candidate `active_intent.model_copy(update={"candidate_ids": intersection})` fallback at line 135 are preserved verbatim. The function `_resolve_order_line_selection` and its public sibling `resolve_order_line_selection` are unchanged.

## 3. Rewrite the Product Modification Resolver Source Intersection

- [x] 3.1 In `backend/intents/context/product_modification_resolver.py`, replace the `set(int(x) for x in recognized_pp_ids)` expression at line 160 with `{int(x) for x in recognized_pp_ids}`. The replacement is expression-equivalent: both constructs produce a `set[int]` with the same membership and the same semantics under the `&` intersection that follows.
- [x] 3.2 In `backend/intents/context/product_modification_resolver.py`, replace the `set(int(x) for x in source_candidate_ids)` expression at line 161 with `{int(x) for x in source_candidate_ids}`. The replacement is expression-equivalent: both constructs produce a `set[int]` with the same membership and the same semantics under the `&` intersection that precedes them.
- [x] 3.3 Confirm the surrounding `sorted(...)` wrapper at lines 159-162, the `if not intersection: return active_intent.model_copy(update={"status": "rejected"})` early-return at lines 163-164, the `if len(intersection) == 1: source_id = intersection[0]` branch at lines 168-186, and the multi-candidate `_build_pending_intent(...)` fallback at lines 188-195 are preserved verbatim. The function `_resolve_source_selection` is unchanged.

## 4. Rewrite the Product Modification Resolver Destination Intersection

- [x] 4.1 In `backend/intents/context/product_modification_resolver.py`, replace the `set(int(x) for x in recognized_dest_ids)` expression at line 220 with `{int(x) for x in recognized_dest_ids}`. The replacement is expression-equivalent: both constructs produce a `set[int]` with the same membership and the same semantics under the `&` intersection that follows.
- [x] 4.2 In `backend/intents/context/product_modification_resolver.py`, replace the `set(int(x) for x in destination_candidate_ids)` expression at line 221 with `{int(x) for x in destination_candidate_ids}`. The replacement is expression-equivalent: both constructs produce a `set[int]` with the same membership and the same semantics under the `&` intersection that precedes them.
- [x] 4.3 Confirm the surrounding `sorted(...)` wrapper at lines 219-222, the `if not intersection: return active_intent.model_copy(update={"status": "rejected"})` early-return at lines 223-224, the `if len(intersection) == 1: dest_id = intersection[0]` branch at lines 228+, and the multi-candidate `_build_pending_intent(...)` fallback are preserved verbatim. The function `_resolve_destination_selection` is unchanged.

## 5. Verification (Ruff)

- [x] 5.1 Run `PYTHONPATH=. venv/bin/python -m ruff check backend --select C401 --output-format=concise` from the repo root and confirm the output is empty (zero findings). The previous six findings at `backend/intents/context/order_line_selection_resolver.py:125`, `:126` and `backend/intents/context/product_modification_resolver.py:160`, `:161`, `:220`, `:221` MUST be gone.
- [x] 5.2 Run `PYTHONPATH=. venv/bin/python -m ruff check backend/intents/context/order_line_selection_resolver.py backend/intents/context/product_modification_resolver.py` (without `--select`) from the repo root and confirm the two files report zero findings across all enabled rules. The rewrite MUST NOT introduce a new finding under any rule (e.g., `E711`, `E712`, `F401`, `I001`, `UP`, `B`, `SIM`, etc.).
- [x] 5.3 Run `PYTHONPATH=. venv/bin/python -m ruff check backend --select C401,E,W,F --output-format=concise` from the repo root and confirm zero `C401` findings remain anywhere in `backend/`. The full ruff coverage MUST also be green for the two touched files.

## 6. Verification (Mypy)

- [x] 6.1 Run `PYTHONPATH=. venv/bin/python -m mypy --strict backend/intents/context/order_line_selection_resolver.py` from the repo root and confirm the file is type-clean. The pre-existing `mypy --strict` status of the file (clean under Subphase 4.11.4 / 4.11.5 baselines) MUST be preserved exactly.
- [x] 6.2 Run `PYTHONPATH=. venv/bin/python -m mypy --strict backend/intents/context/product_modification_resolver.py` from the repo root and confirm the file is type-clean. The pre-existing `mypy --strict` status of the file MUST be preserved exactly.
- [x] 6.3 Run `PYTHONPATH=. venv/bin/python -m mypy --strict backend/intents/context/` from the repo root and confirm zero new errors are introduced in either of the touched files or any of their sibling context resolvers. Pre-existing unrelated errors (if any) are documented separately and MUST NOT block this change.

## 7. Verification (Compile)

- [x] 7.1 Run `PYTHONPATH=. venv/bin/python -m compileall backend/intents/context/order_line_selection_resolver.py backend/intents/context/product_modification_resolver.py` from the repo root and confirm exit code `0`. The two files MUST compile byte-for-byte under CPython's standard library bytecode compiler.
- [x] 7.2 Run `PYTHONPATH=. venv/bin/python -m compileall backend/intents/context/` from the repo root and confirm exit code `0`. All sibling context resolvers MUST remain byte-compilable.

## 8. Verification (Test Surface)

- [x] 8.1 Run `PYTHONPATH=. venv/bin/pytest backend/tests/test_product_recognition_calibration_4_11_5.py -vv` from the repo root and confirm the suite remains green (46 tests + 15 subtests, 100% pass, 0 failures, 0 errors, 0 skips). The pre-rewrite baseline documented in the Subphase 4.11.5 archive is the reference.
- [x] 8.2 Run `PYTHONPATH=. venv/bin/pytest backend/tests/test_product_recognition_calibration_4_11_4.py backend/tests/test_product_recognition_calibration_4_11_3.py backend/tests/test_product_recognition_calibration_runner.py backend/tests/test_product_recognition_calibration_dataset_4_11_1.py backend/tests/test_product_recognition_calibration_eligibility.py backend/tests/test_product_recognition_calibration_report.py backend/tests/test_product_recognition_calibration_policy.py backend/tests/test_product_recognition_calibration_cli.py backend/tests/test_product_recognition_calibration_commerce_catalog.py backend/tests/test_product_recognition_calibration_inventory_4_11_4.py -vv` from the repo root and confirm every Subphase 4.11–4.11.4 calibration suite remains green. The cumulative Subphase 4.11–4.11.5 baseline (193 tests + 15 subtests) MUST be preserved.
- [x] 8.3 Run `PYTHONPATH=. venv/bin/pytest backend/tests/ -vv` from the repo root and confirm zero regressions across the full backend test suite. Any pre-existing unrelated failure is reported separately and MUST NOT block this change.
- [x] 8.4 Confirm no test signature, fixture, parametrisation, or expectation was modified to keep the suite green. The rewrite is fully expression-equivalent — every existing test exercises the rewritten path without modification.

## 9. Verification (OpenSpec)

- [x] 9.1 Run `cd . && openspec validate subphase-4-11-6-resolve-remaining-ruff-c401-findings --strict` and confirm the change bundle remains valid. The proposal, design, and tasks artifacts MUST validate as `valid: true`. The single capability `ruff-c401-closure-4-11-6` MUST satisfy the schema's `ADDED Requirements` + `REMOVED Requirements` formatting.
- [x] 9.2 Run `cd . && openspec list --json` and confirm the change is listed as the only active change (no other active change exists in the repo) and its name, schema, and `applyRequires` matches the documented state.

## 10. Reporting

- [x] 10.1 Report the change summary: a 6-line mechanical rewrite that closes the Subphase 4.11 calibration chain by replacing `set(int(value) for value in iterable)` with `{int(value) for value in iterable}` at the three intersection sites in `backend/intents/context/order_line_selection_resolver.py` and `backend/intents/context/product_modification_resolver.py`. Report the exact `git diff` (6 single-line edits, 12 changed lines / 6 added / 6 removed, no whitespace-only or comment-only changes), the exact `ruff check --select C401` output (zero findings across the full backend surface), the exact `mypy --strict` output for the two touched files (clean), and the exact pytest run command and pass count (193 + 15 baseline preserved).
- [x] 10.2 Update `openspec/specs/project.md` to replace the pending `### # Subphase 4.11.6 — Resolve Remaining Ruff C401 Findings [ ]` entry with the proper `### Subphase 4.11.6 — Resolve Remaining Ruff C401 Findings [x] — completed` entry following the exact structure used by the Subphase 4.11.5 / 4.11.4 / 4.11.3 archived entries (objective, context, completed outcomes, architectural constraints, relevant files, OpenSpec, context for future subphases). Document the 6-line diff, the two files touched, the three intersection sites rewritten, the `ruff check --select C401` zero-finding closure, the `mypy --strict` clean status, the 193 + 15 test baseline, and the absence of any recognizer / calibration / runtime / dataset change.
- [x] 10.3 Leave the change under `openspec/changes/subphase-4-11-6-resolve-remaining-ruff-c401-findings/`; do not run specification sync or archive. The user will run `/opsx:sync` (to merge the `ruff-c401-closure-4-11-6` capability into `openspec/specs/`) and `/opsx:archive` (to move the change bundle under `archive/`) as separate explicit commands after the report is reviewed.
