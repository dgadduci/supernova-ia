## 1. Response Orchestrator Implementation

- [x] 1.1 Create `backend/intents/orchestration/incoming_message_response_orchestrator.py` with `__all__ = ["process_incoming_message_with_responses"]` and the typed-alias imports: `Session as DatabaseSession` from `sqlalchemy.orm`, `Session as ConversationSession` from `backend.models.session`, `CustomerResponse` from `backend.intents.schemas.customer_response`, `ProcessedIntent` from `backend.intents.schemas.processed_intent`, `process_incoming_message_transactional` from `backend.intents.orchestration.transactional_message_processor`, and `build_agregar_producto_response` from `backend.intents.responses.agregar_producto_response`.
- [x] 1.2 Declare a single module-level `GENERIC_MESSAGE` constant: a fixed Spanish apology sentence with no placeholders, no formatting parameters, no per-call substitution, and no exposure of IDs, exception types, stack traces, or technical detail.
- [x] 1.3 Implement `process_incoming_message_with_responses(db, session, message)` so the first call is `processed = process_incoming_message_transactional(db, session, message)` — exactly once, with the same `db`, `session`, and `message` arguments; the wrapper SHALL NOT re-validate the message, re-route on `session.context_type`, or invoke any dispatcher / orchestrator / handler directly.
- [x] 1.4 Implement the per-`ProcessedIntent` dispatch as a literal `if/elif` chain: when `intent.intent == "agregar_producto"`, append the `CustomerResponse` returned by `build_agregar_producto_response(db, session, intent)` (called exactly once per item); for any other `intent`, append `CustomerResponse(message=GENERIC_MESSAGE, intent=intent.intent, status=intent.status)` preserving the original `intent` and `status`. Preserve the inner list order; the returned list length SHALL equal the inner list length.
- [x] 1.5 Ensure the wrapper never catches or re-raises the inner exception; the `return` statement sits directly after the loop so `process_incoming_message_transactional` exceptions propagate out unchanged. Never wrap, convert, swallow, or translate to `HTTPException`.

## 2. Module Boundaries

- [x] 2.1 Keep the response orchestrator module free of `sqlalchemy.select` / `sqlalchemy.orm.joinedload` / `backend.repositories.*` / `backend.services.*` / `backend.intents.handlers.*` / `backend.intents.context.*` / `backend.intents.recognizers.*` / `backend.intents.resolvers.*` / `backend.intents.processor` / `backend.intents.contracts.*` / `backend.intents.services.*` / `backend.llm.*` / `backend.routers.*` / `backend.dependencies` / `backend.sessions` / `backend.old_project.*` / `requests` / `fastapi` / `twilio` imports.
- [x] 2.2 Do not call `db.commit`, `db.rollback`, `db.flush`, `db.refresh`, `db.expire`, or `db.begin` in the wrapper; the only database access is the read `build_agregar_producto_response` makes internally through `ProductoQueryService.list_presentaciones_by_ids`.
- [x] 2.3 Do not introduce logging, retry / backoff, async wrappers, caching, locale selection, template engines, response beautification, response objects for additional intents, queue promotion, or HTTP / Twilio integration.

## 3. Test Implementation

- [x] 3.1 Create `backend/tests/test_incoming_message_response_orchestrator.py` using `unittest.TestCase`. At module load time, import `backend.intents.orchestration.incoming_message_response_orchestrator as response_module` so the patches target the wrapper's import site. Use `unittest.mock.patch.object(response_module, "process_incoming_message_transactional", ...)` and `unittest.mock.patch.object(response_module, "build_agregar_producto_response", ...)`; do NOT patch `backend.intents.orchestration.transactional_message_processor` or `backend.intents.responses.agregar_producto_response` directly.
- [x] 3.2 `agregar_producto` executed test: configure `process_incoming_message_transactional` to return a one-item `[ProcessedIntent(intent="agregar_producto", source_text="...", status="executed", handler="agregar_producto", recognizer="recognizer_productos", resolved_data={"producto_presentacion_id": 1, "cantidad": 2})]`; configure `build_agregar_producto_response` to return `CustomerResponse(message="Listo, agregué 2 Pizza Mozzarella grande.", intent="agregar_producto", status="executed")`. Assert that `build_agregar_producto_response.assert_called_once_with(db, session, processed)` and that the wrapper returns `[builder_response]` (identity-preserving).
- [x] 3.3 `agregar_producto` pending_resolution test: same as 3.2 but with `status="pending_resolution"`, `candidate_ids=[1, 2]`, `resolved_data={}`; configure the builder mock to return a `CustomerResponse(message="Elegí entre: ...", intent="agregar_producto", status="pending_resolution")`. Assert `build_agregar_producto_response.assert_called_once_with(db, session, processed)` and that the wrapper returns the builder's response unchanged.
- [x] 3.4 `agregar_producto` rejected test: same as 3.2 but with `status="rejected"`; configure the builder mock to return a `CustomerResponse` with `status="rejected"`. Assert the wrapper returns the builder's response unchanged.
- [x] 3.5 `agregar_producto` failed test: same as 3.2 but with `status="failed"`; configure the builder mock to return a `CustomerResponse` with `status="failed"`. Assert the wrapper returns the builder's response unchanged.
- [x] 3.6 Unsupported-intent `desconocida` test: configure the inner call to return `[ProcessedIntent(intent="desconocida", source_text="asdfgh", status="rejected", handler="desconocida", recognizer="intent_classifier")]`. Assert `build_agregar_producto_response.assert_not_called()` and the wrapper returns `[CustomerResponse(message=GENERIC_MESSAGE, intent="desconocida", status="rejected")]`.
- [x] 3.7 Unsupported-intent `saludo` test: same as 3.6 but with `intent="saludo"` and `status="rejected"`; assert `build_agregar_producto_response.assert_not_called()` and the wrapper returns a `CustomerResponse` with `intent="saludo"` and `status="rejected"` and `message == GENERIC_MESSAGE`.
- [x] 3.8 Unsupported-intent `consultar_pedido` (any non-`agregar_producto` future intent) test: configure the inner call to return `[ProcessedIntent(intent="consultar_pedido", source_text="...", status="executed", handler="consultar_pedido", recognizer="intent_classifier", resolved_data={})]`. Assert `build_agregar_producto_response.assert_not_called()` and the wrapper returns `[CustomerResponse(message=GENERIC_MESSAGE, intent="consultar_pedido", status="executed")]`.
- [x] 3.9 Generic-message determinism test: assert `GENERIC_MESSAGE` is a single fixed string; assert it does NOT contain `"id"`, `"Exception"`, `"Traceback"`, or `"Error"`; assert it is non-empty.
- [x] 3.10 Multi-intent order test: configure the inner call to return three `ProcessedIntent` items in the order `[pending_resolution (agregar_producto), executed (agregar_producto), rejected (desconocida)]`. Configure the builder mock to return distinguishable `CustomerResponse` instances for the first two items (e.g. `"CLARIFICATION"` and `"CONFIRMATION"`). Assert the wrapper returns a three-item list in the same order, with positions 0 and 1 produced by the builder mock (each called exactly once with the matching `ProcessedIntent`) and position 2 produced by the generic fallback with `intent="desconocida"` and `status="rejected"`.
- [x] 3.11 Single-intent length test: configure the inner call to return a one-item list; assert the wrapper returns a one-item list; configure the inner call to return an empty list (the pending-context branch); assert the wrapper returns an empty list and `build_agregar_producto_response.assert_not_called()`.
- [x] 3.12 ValueError propagation test: configure `process_incoming_message_transactional` to raise `ValueError("message must be a non-empty, non-whitespace string")`. Invoke the wrapper with `message=""`; assert `ValueError` propagates out, the original message text is preserved (`str(exc) == "message must be a non-empty, non-whitespace string"`), and `build_agregar_producto_response.assert_not_called()`.
- [x] 3.13 TypeError propagation test: configure `process_incoming_message_transactional` to raise `TypeError("message must be a str")`. Invoke the wrapper with `message=None`; assert `TypeError` propagates out with the same type and message.
- [x] 3.14 QueryLlmError propagation test: configure `process_incoming_message_transactional` to raise a `QueryLlmTimeoutError` instance. Invoke the wrapper; assert the SAME exception instance propagates out (`is` identity check), the original traceback frames remain attached (the wrapper's frame is NOT in `exc.__traceback__`), and no `CustomerResponse` is returned.
- [x] 3.15 pydantic ValidationError propagation test: configure `process_incoming_message_transactional` to raise `pydantic.ValidationError`. Invoke the wrapper; assert the same exception instance propagates out.
- [x] 3.16 `__all__` discipline test: import `backend.intents.orchestration.incoming_message_response_orchestrator`; assert `module.__all__ == ["process_incoming_message_with_responses"]`.
- [x] 3.17 No additional public functions test: inspect the module source and assert it defines exactly one top-level `def` statement (`process_incoming_message_with_responses`); private constants and imports are permitted.
- [x] 3.18 No-mutation / no-commit / no-rollback test: invoke the wrapper with a `MagicMock(name="DatabaseSession")` whose `commit`, `rollback`, `flush`, `refresh`, `expire`, `begin` are also mocks; assert `db.commit.assert_not_called()`, `db.rollback.assert_not_called()`, `db.flush.assert_not_called()`, `db.refresh.assert_not_called()`, `db.expire.assert_not_called()`, `db.begin.assert_not_called()` after both a successful branch (single `agregar_producto` intent) and an exception branch (`process_incoming_message_transactional` raises `ValueError`).
- [x] 3.19 No session / intent mutation test: snapshot `session.pending_intents`, `session.context_type`, `session.id_pedido`, and the entire `ProcessedIntent` model before invocation; invoke the wrapper for one `agregar_producto` (using a real `ProcessedIntent` instance) and one unsupported-intent branch; assert the snapshots are unchanged after the call.
- [x] 3.20 Module source-code boundary test: read `backend/intents/orchestration/incoming_message_response_orchestrator.py` from disk; assert the source does NOT contain `"from sqlalchemy import select"`, `"joinedload"`, `"from backend.repositories"`, `"from backend.services"`, `"from backend.intents.handlers"`, `"from backend.intents.context"`, `"from backend.intents.recognizers"`, `"from backend.intents.resolvers"`, `"from backend.intents.processor"`, `"from backend.intents.contracts"`, `"from backend.intents.services"`, `"from backend.llm"`, `"from backend.routers"`, `"from backend.dependencies"`, `"from backend.sessions"`, `"from backend.old_project"`, `"import requests"`, `"import fastapi"`, `"import twilio"`, `"HTTPException"`, `"JSONResponse"`, `"MessagingResponse"`, `"QueryLlm"`, `"retry"`, `"backoff"`, `"asyncio"`, `"async def"`, `"logger."`, `"logging."`, `"print("`, or `"time.sleep"`.
- [x] 3.21 Wire `TestIncomingMessageResponseOrchestrator` into the standard test runner pattern used by `test_incoming_message_orchestrator.py` / `test_transactional_message_processor.py` / `test_initial_intent_dispatcher.py` (a `unittest.main(verbosity=2)` block guarded by `if __name__ == "__main__":`).

## 4. Verification

- [x] 4.1 Run `PYTHONPATH=. venv/bin/python backend/tests/test_incoming_message_response_orchestrator.py` and confirm every assertion passes without a real LLM, database, or HTTP.
- [x] 4.2 Run `PYTHONPATH=. venv/bin/python -m compileall backend` and confirm exit 0.
- [x] 4.3 Run `openspec validate incoming-message-response-orchestrator-3-28 --strict` and confirm valid.
- [x] 4.4 Roll back by deleting `backend/intents/orchestration/incoming_message_response_orchestrator.py` and `backend/tests/test_incoming_message_response_orchestrator.py`; no other module imports `process_incoming_message_with_responses`, so no ripple effects.