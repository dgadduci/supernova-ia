## 1. Infrastructure

- [x] 1.1 Install FastAPI, Uvicorn, and httpx (test client) into the project `venv` if not already present
- [x] 1.2 Create `backend/dependencies.py` exposing a `get_session` generator that yields a SQLAlchemy session and closes it after the request
- [x] 1.3 Create `backend/main.py` that constructs the FastAPI app, registers the health router, registers the comercios router, and exposes `/docs`

## 2. Health endpoint

- [x] 2.1 Create `backend/routers/health.py` with `GET /health` returning `{"status": "ok"}` (no DB access)
- [x] 2.2 Verify `/health` returns 200 via `curl` after `uvicorn backend.main:app` is started locally

## 3. Commerce schemas

- [x] 3.1 Create `backend/schemas/comercio.py` defining `ComercioCreate` (no lifecycle fields; `zona_horaria`/`moneda`/`idioma` optional) and `ComercioResponse` (full persisted column set, excluding `metodos_entrega` and `medios_pago` associations)
- [x] 3.2 Configure `ComercioResponse` so it can be populated from a SQLAlchemy `Comercio` ORM instance (`from_attributes=True` or equivalent)

## 4. Commerce repository

- [x] 4.1 Create `backend/repositories/comercio_repository.py` with `list_all`, `get_by_id`, `get_by_whatsapp`, `get_by_slug`, `create`, and `estado_exists`
- [x] 4.2 Confirm the repository uses SQLAlchemy ORM or `select()` statements only; no `commit()` or `rollback()` calls

## 5. Commerce service

- [x] 5.1 Create `backend/services/exceptions.py` with `ComercioNotFound`, `EstadoComercioNotFound`, `DuplicateWhatsapp`, and `DuplicateSlug`
- [x] 5.2 Create `backend/services/comercio_service.py` with `list_all`, `get_by_id`, and `create`. `create` must trim text whitespace, reject empty required strings, verify estado existence via the repository, check whatsapp and slug uniqueness via the repository, raise the appropriate domain exception, commit on success, and roll back on any DB error

## 6. Commerce router

- [x] 6.1 Create `backend/routers/comercios.py` with `GET /comercios`, `GET /comercios/{comercio_id}`, and `POST /comercios`. Each endpoint declares the session dependency and the service dependency
- [x] 6.2 Register the router in `backend/main.py`
- [x] 6.3 Add exception handlers (or `try/except` blocks in the endpoints) that map the four domain exceptions to the documented HTTP status codes (`404`, `409`, plus `201` on success)

## 7. Minimum integration tests

- [x] 7.1 Add the minimum test client fixture and a fixture that overrides the session dependency to point at `supernova_test`
- [x] 7.2 Test: `GET /health` returns 200 and `{"status": "ok"}`
- [x] 7.3 Test: `POST /comercios` creates a row and returns 201 with the new id
- [x] 7.4 Test: `GET /comercios/{id}` returns the row created in 7.3
- [x] 7.5 Test: `GET /comercios` returns the existing rows ordered by id
- [x] 7.6 Test: `GET /comercios/{id}` returns 404 for a missing id
- [x] 7.7 Test: `POST /comercios` returns 404 when `estado_id` does not exist
- [x] 7.8 Test: `POST /comercios` returns 409 when `whatsapp` already exists
- [x] 7.9 Test: `POST /comercios` returns 409 when `slug` already exists
- [x] 7.10 Run the full integration test suite against `supernova_test` and confirm all tests pass

## 8. Completion check

- [x] 8.1 Start Uvicorn locally (`uvicorn backend.main:app --reload`) and confirm `/docs` loads in a browser
- [x] 8.2 Confirm no Alembic migration was generated by this change (`alembic revision --autogenerate` produces no diff)
- [x] 8.3 Confirm no SQLAlchemy model file was modified by this change
