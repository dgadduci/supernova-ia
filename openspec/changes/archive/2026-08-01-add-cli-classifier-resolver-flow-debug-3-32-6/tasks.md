## 1. Backend Diagnostics Module

- [x] 1.1 Create `backend/diagnostics/__init__.py` exporting the public surface: `DiagnosticSink`, `NoopDiagnosticSink`, `CollectingDiagnosticSink`, `serialize`, `redact`, and the five event dataclasses (`ClassifierCallStarted`, `ClassifierCallCompleted`, `ResolverCallStarted`, `ResolverCallCompleted`, `PendingStateSnapshot`).
- [x] 1.2 Add `backend/diagnostics/events.py` with the five dataclass event types. Each event has the fields listed in design D3 and a `to_dict()` method that calls `serialize(self)`. Each event has a `phase` field (`"classifier"`, `"resolver"`, or `"pending"`) and a `sequence` field populated by the sink at emit time.
- [x] 1.3 Add `backend/diagnostics/sink.py` with the `DiagnosticSink` protocol (`on_classifier_started`, `on_classifier_completed`, `on_resolver_started`, `on_resolver_completed`, `on_pending_state_snapshot`), the `NoopDiagnosticSink` (final class with empty methods), and the `CollectingDiagnosticSink` (records events in a list, allocates `CLS-NNN` / `RES-NNN` call IDs, exposes `events()` and `clear()`).
- [x] 1.4 Add `backend/diagnostics/serializer.py` with the `serialize(value, *, redact=True, _seen=None) -> object` function. Implement handlers for `None`, `bool`, `int`, `float`, `str`, `Enum`, `dict`, `list`/`tuple`/`set`/`frozenset`, dataclasses, Pydantic v2 models, and SQLAlchemy ORM instances whose class has a `__table__` attribute. Fall back to `"<ClassName>"` for unsupported types. Bound recursion depth at 64 and total field count at 4096 per call. Reject duplicate-object cycles via `_seen`.
- [x] 1.5 Add `backend/diagnostics/redaction.py` with the module-level constant `_REDACTED_KEYS = {"password", "token", "api_key", "authorization", "secret", "database_url", "DATABASE_URL", "Authorization", "X-API-Key", "X-API-KEY"}` and a `redact(value: object) -> object` function that walks `dict`/`list`/`tuple` and replaces values whose key (case-insensitive) is in `_REDACTED_KEYS` with the string `"<redacted>"`. Reuse from `serialize` when `redact=True`.
- [x] 1.6 Confirm the module is importable from a fresh shell with `PYTHONPATH=. .venv/bin/python -c "from backend.diagnostics import DiagnosticSink, NoopDiagnosticSink, CollectingDiagnosticSink, serialize, redact; print('ok')"` and exits 0.

## 2. Instrument Classifier and Resolvers

- [x] 2.1 Add a `sink: DiagnosticSink = NoopDiagnosticSink()` parameter to `IntentClassifier.__init__` (default to `NoopDiagnosticSink()`). Keep the existing `QueryLlm` injection unchanged. The constructor stores the sink and exposes no new public method.
- [x] 2.2 Wrap the `self._query_llm.request(prompt)` call inside `IntentClassifier.query` with a `try / finally` block that emits `ClassifierCallStarted` before the call and `ClassifierCallCompleted` after the call (and after `IntentClassificationResult.model_validate`). The `Completed` event includes the validated Pydantic result, the intent count, the unknown fragment list, and the parse errors. The `Started` event includes the raw message, the active context (passed by the caller), and the prompt metadata. The query LLM call still happens exactly once.
- [x] 2.3 Add a `sink: DiagnosticSink = NoopDiagnosticSink()` keyword-only argument to `ProductSelectionContextResolver.resolve` (and the equivalent resolver entry points invoked in the drain-and-promote loop). Wrap the real resolution body with `sink.on_resolver_started(...)` and `sink.on_resolver_completed(...)`. The `Started` event captures the input intent, source text, quantity, status, requirements, candidate IDs, and the candidate catalog. The `Completed` event captures the new status, the selected candidate, the remaining candidate IDs, the requirements, the resolved_data, the rejection reason, and the clarification message.
- [x] 2.4 Add `sink` arguments to the pending-context resolver method (the equivalent of `execute_pending_context` or the function invoked from `dispatch_pending_context`). Emit the same `ResolverCallStarted` / `ResolverCallCompleted` events. The `pending_context_state` is captured in a `PendingStateSnapshot` event before the resolver call and after each resolver call.
- [x] 2.5 Add `sink` arguments to the resolver invoked after a queued intent is promoted (the drain-and-promote loop in `process_incoming_message`). Emit the same `ResolverCallStarted` / `ResolverCallCompleted` events. The `RES-N` sequence number is global per request, so the events for the first call, the promotion, and the second call are sorted by sequence.
- [x] 2.6 Add `sink` arguments to the `modificar_producto` resolver when it shares the same diagnostic mechanism (the existing resolver instance is reused; the sink emits `RES-N` events for each call). Do not introduce a new resolver class. Do not change the matching or threshold rules.
- [x] 2.7 Confirm with a focused unit test that the total number of `QueryLlm.request` calls does not change when a `CollectingDiagnosticSink` is active (assertion on the stub's call count). The classifier must not re-classify.

## 3. Wire Up the `X-Debug-Flow` Header

- [x] 3.1 Add a FastAPI dependency `get_diagnostic_sink(x_debug_flow: str | None = Header(default=None, alias="X-Debug-Flow")) -> DiagnosticSink` to `backend/routers/incoming_messages.py` (or a new `backend/diagnostics/fastapi.py` helper). When `x_debug_flow` is truthy, return a `CollectingDiagnosticSink`. Otherwise return `NoopDiagnosticSink()`. Document the dependency in `__all__`.
- [x] 3.2 Inject the sink into the incoming-message service call in the router. The service signature accepts an optional `sink` keyword argument that defaults to `NoopDiagnosticSink()`. Existing callers in tests that do not pass the keyword still work. The router builds the dependencies and passes the sink through.
- [x] 3.3 Modify the existing response builder in `backend/routers/incoming_messages.py` to merge the sink's collected events into the response payload under the `diagnostics` key when the sink is a `CollectingDiagnosticSink`. The default response (no header) keeps the exact same shape and does not include a `diagnostics` key. The events are sorted by `(sequence, phase)` tuple before serialization.
- [x] 3.4 Add a `redact` pass on the response payload before it returns. The pass walks the payload (including the `diagnostics` field) and replaces values whose key is in `_REDACTED_KEYS` with `<redacted>`. The source URL surface (`base_url`) is preserved, but the full `DATABASE_URL` is redacted.
- [x] 3.5 Confirm with a focused API test that the default response (no `X-Debug-Flow` header) is byte-for-byte identical to the pre-change response: count keys, count response list length, and assert the `responses` payload is unchanged. Confirm with a second test that the response with `X-Debug-Flow: 1` includes a `diagnostics` list whose length matches the sink's event count.

## 4. CLI Flag Parsing and Debug Renderer

- [x] 4.1 Add `--debug-flow` (`action="store_true"`) and `--debug-components` (default `""`) to the `argparse.ArgumentParser` constructed in `main()` of `backend/scripts/cli_chat_client.py`. The `--base-url` flag moves to the same parser. Add a custom usage line that reflects the new flags.
- [x] 4.2 Add a module-level constant `_DEBUG_COMPONENT_ALIASES = {"classifier", "resolver", "pending"}` and a helper `_parse_debug_components(raw: str) -> frozenset[str]` that splits on commas, strips whitespace, returns `frozenset(_DEBUG_COMPONENT_ALIASES)` when `raw` is empty, and raises `SystemExit(2)` with a clear error when any value is not in `_DEBUG_COMPONENT_ALIASES`. Call the helper at the start of `main()` and store the result in a local variable.
- [x] 4.3 Add a header dict to the incoming-messages POST request when `--debug-flow` is enabled: `{"X-Debug-Flow": "1"}`. The header is included only when the flag is set. When the flag is not set, the request dict is exactly the same as before.
- [x] 4.4 Add a new private helper `_extract_diagnostics(payload: dict) -> list[dict]` that returns `payload.get("diagnostics", [])` when it is a `list`, otherwise `[]`. Add a new helper `_render_diagnostics(components: frozenset[str], events: list[dict]) -> None` that iterates the events, filters by `phase` against `components`, and prints the appropriate table per event type.
- [x] 4.5 Add `_format_kv_table(title: str, rows: list[tuple[str, str]]) -> str`, `_format_intent_table(title: str, headers: tuple[str, ...], rows: list[tuple[str, ...]]) -> str`, `_format_pending_state_snapshot(title: str, snapshot: dict) -> str`, and `_format_pending_queue_table(title: str, queue: list[dict]) -> str` to `backend/scripts/cli_chat_client.py`. The helpers use the same stdlib-only style as the existing `format_order_table` (dynamic column widths, `+---+---+` borders, left-padded string columns, right-padded numeric columns). Add the names to `__all__`.
- [x] 4.6 Add a `_redact_payload(payload: dict | list) -> dict | list` helper that walks the payload and replaces values whose key (case-insensitive) is in `_REDACTED_KEYS` with `<redacted>`. The helper is invoked once on every payload before rendering. Define `_REDACTED_KEYS` as a module-level constant mirroring the backend's set.
- [x] 4.7 Modify `main()` in `backend/scripts/cli_chat_client.py`. After the existing `_print_responses(payload)` call, when `--debug-flow` is enabled and the payload contains a `diagnostics` list, call `_render_diagnostics(components, _extract_diagnostics(payload))`. The `try / finally` cleanup of the session, the `exit` handling, the empty-input skipping, the bootstrap, and the order table printing remain unchanged. The diagnostic tables are printed between the customer responses and the order table.

## 5. CLI Tests

- [x] 5.1 Add a `test_debug_flow_flag_disabled_keeps_default_output` test that drives the CLI without `--debug-flow`. The test sends a `agregar_producto` request, asserts the response is printed, asserts no `CLASSIFIER INPUT` or `RESOLVER INPUT` title appears in the captured stdout, and asserts the request payload did not include the `X-Debug-Flow` header.
- [x] 5.2 Add a `test_debug_flow_flag_enabled_sends_header_and_renders_tables` test that drives the CLI with `--debug-flow`. The test sends a `agregar_producto` request where the fake response includes a `diagnostics` field with one `CLASSIFIER INPUT` event and one `CLASSIFIER OUTPUT` event. The test asserts the header is sent, the tables are printed in arrival order, and the customer response is printed before the tables.
- [x] 5.3 Add a `test_debug_components_filter_limits_output` test that drives the CLI with `--debug-flow --debug-components classifier`. The test sends a response with both `classifier` and `resolver` events. The test asserts the `CLASSIFIER INPUT` and `CLASSIFIER OUTPUT` tables are printed and the `RESOLVER INPUT` table is not printed.
- [x] 5.4 Add a `test_debug_components_unknown_value_exits_with_error` test that drives the CLI with `--debug-flow --debug-components foo`. The test asserts the CLI exits with code 2 and prints a clear error message containing the unknown value.
- [x] 5.5 Add a `test_debug_flow_redacts_secret_fields` test that drives the CLI with `--debug-flow` and a fake response where a `diagnostics` event contains a `password` key. The test asserts the rendered table contains `<redacted>` and never contains the literal password string.
- [x] 5.6 Add a `test_debug_flow_renders_classifier_output_with_multiple_intents` test that drives the CLI with a fake response containing a `CLASSIFIER OUTPUT` event with two classified intents. The test asserts both intents appear in the rendered table, in source order, with correct fields (intent name, source text, quantity, confidence, status, resolved_data, requirements, candidate IDs, raw).
- [x] 5.7 Add a `test_debug_flow_renders_resolver_candidates_table` test that drives the CLI with a fake response containing a `RESOLVER CANDIDATES` event with three candidate rows. The test asserts all three rows are rendered, with the candidate IDs, product names, presentation codes, and `activo` / `disponible` flags.
- [x] 5.8 Add a `test_debug_flow_renders_pending_state_snapshot` test that drives the CLI with a fake response containing a `PENDING STATE` event. The test asserts the snapshot is rendered with the active intent, active status, queue length, queue intents, and context type.
- [x] 5.9 Add a `test_debug_flow_renders_pending_queue_table` test that drives the CLI with a fake response containing a `PENDING QUEUE` event with two queued intents. The test asserts both rows are rendered in queue order, with intent name, status, source text, quantity, candidate IDs, requirements, and resolved data.
- [x] 5.10 Add a `test_debug_flow_renders_resolver_matches_table` test that drives the CLI with a fake response containing a `RESOLVER MATCHES` event with three matches. The test asserts all three matches are rendered with the candidate ID, candidate, score, match type, and matched text.
- [x] 5.11 Add a `test_debug_flow_call_id_correlation` test that drives the CLI with a fake response containing a `RESOLVER INPUT` event followed by a `RESOLVER OUTPUT` event with the same `call_id` (`RES-001`). The test asserts both tables are rendered with the same `RES-001` ID in the title.
- [x] 5.12 Add a `test_debug_flow_unicode_preserved` test that drives the CLI with a fake response containing a `CLASSIFIER OUTPUT` event whose values include accented product names (`Empanada de Jamón y Queso`, `Pizza de Muzzarella`). The test asserts the rendered table contains the accented characters verbatim (no `u00f3` escape, no truncation).
- [x] 5.13 Add a `test_debug_flow_error_path_prints_error_table` test that drives the CLI with a fake response containing a `CLASSIFIER COMPLETED` event whose `result_type` is `ClassifierError` and whose `parse_errors` contains a `ValueError`. The test asserts the error table is rendered with the exception type and the input that caused the failure.
- [x] 5.14 Add a `test_debug_flow_no_duplicate_classifier_or_resolver_calls` test that drives the CLI with `--debug-flow --debug-flow` (or any combination that could plausibly trigger duplicate emission) and asserts the backend stub's `QueryLlm.request` call count matches the `CLS-N` count. The CLI does not invoke the classifier or any resolver.
- [x] 5.15 Add a `test_debug_flow_does_not_affect_existing_business_flow` test that drives the CLI with `--debug-flow` and asserts the existing order-table branch (`response_modified_order` returns `True`) still triggers exactly one `GET /pedidos/{pedido_id}/detalle` and the table is printed unchanged. The diagnostic tables are printed before the order table.

## 6. Backend Tests

- [x] 6.1 Add a `backend/tests/test_diagnostics.py` module with focused serializer tests: `test_serialize_primitive_types`, `test_serialize_dict_sorts_keys`, `test_serialize_list_order_preserved`, `test_serialize_dataclass_fields`, `test_serialize_pydantic_model_fields`, `test_serialize_enum_uses_value`, `test_serialize_sqlalchemy_orm_instance_reads_columns`, `test_serialize_unsupported_returns_classname`, `test_serialize_recursion_loop_returns_classname`, `test_serialize_depth_limit_returns_classname`, `test_redact_password_token_api_key`, `test_redact_database_url`, `test_redact_authorization_header`, `test_redact_nested_in_list`, `test_redact_disabled_passes_through`. Use the existing `supernova_test` fixtures only when a SQLAlchemy ORM instance is needed; otherwise tests are pure functions.
- [x] 6.2 Add a `test_diagnostic_sink_noop_does_nothing` test. Construct a `NoopDiagnosticSink`, emit all five event types, and assert no state is retained (the sink has no readable attribute beyond the methods).
- [x] 6.3 Add a `test_collecting_sink_assigns_sequential_call_ids` test. Construct a `CollectingDiagnosticSink`, emit `on_classifier_started`, `on_classifier_completed`, `on_resolver_started`, `on_resolver_completed` in order, and assert the events carry `CLS-001`, `CLS-001`, `RES-001`, `RES-001` call IDs in the right order.
- [x] 6.4 Add a `test_collecting_sink_events_are_sorted_by_sequence_in_router` test. The router sorts the merged events by `(sequence, phase)` tuple before serialization. The test asserts the order is deterministic.
- [x] 6.5 Add a `test_x_debug_flow_header_activates_sink` test using the FastAPI `TestClient`. Send a request with `X-Debug-Flow: 1` and assert the response includes a `diagnostics` field whose length matches the sink's event count. Send a request without the header and assert the response does not include the `diagnostics` field.
- [x] 6.6 Add a `test_x_debug_flow_response_redacts_secrets` test. Send a request with `X-Debug-Flow: 1` and a payload that would emit a `diagnostics` event containing a `password` field. Assert the response payload does not contain the literal password string and contains `<redacted>` instead.
- [x] 6.7 Add a `test_incoming_message_default_response_unchanged` test. Send a request without the header. Assert the response keys are exactly `{responses}` (or whatever the existing keys are), and the `responses` payload is byte-for-byte identical to the pre-change snapshot.

## 7. Existing Business Regression Tests

- [x] 7.1 Re-run the existing `agregar_producto` end-to-end suite (the test classes that exercise the modern pipeline). Confirm no regression in the happy path, the ambiguous-resolution path, the rejected path, and the failed path. The diagnostic mode is silent by default.
- [x] 7.2 Re-run the existing `quitar_producto` suite. Confirm no regression. The diagnostic mode does not affect the `quitar_producto` resolver.
- [x] 7.3 Re-run the existing `modificar_producto` suite. Confirm no regression. The diagnostic mode emits `RES-N` events for each resolver call but does not change the matching logic.
- [x] 7.4 Re-run the existing pending queue tests (the 3.32.3 / 3.32.4 / 3.32.5 suites). Confirm no regression in queue promotion, FIFO ordering, and queue exhaustion.
- [x] 7.5 Re-run the existing incoming-message HTTP tests (the API smoke tests). Confirm no regression in the default response shape, the status codes, and the error paths.

## 8. Verification

- [x] 8.1 Run `PYTHONPATH=. .venv/bin/python -m compileall backend` from the project root. Expect exit 0.
- [x] 8.2 Run `PYTHONPATH=. .venv/bin/python -m unittest backend.tests.test_diagnostics -v`. Expect all serializer, sink, and event tests to pass.
- [x] 8.3 Run `PYTHONPATH=. .venv/bin/python -m unittest backend.tests.test_cli_chat_client -v`. Expect all CLI tests (including the new diagnostic tests) to pass.
- [x] 8.4 Run `PYTHONPATH=. .venv/bin/python -m unittest backend.tests.api_smoke -v` (or the project's full smoke runner) to confirm no regression in the existing 400+ tests, including the `agregar_producto`, `quitar_producto`, `modificar_producto`, pending queue, and incoming-message HTTP suites.
- [x] 8.5 Run `PYTHONPATH=. .venv/bin/python -m ruff check backend` and `PYTHONPATH=. .venv/bin/python -m mypy backend` (or the project's lint and type-check commands). Fix any failures introduced by this change.
- [x] 8.6 Run `openspec validate add-cli-classifier-resolver-flow-debug-3-32-6 --strict`. Expect valid.
- [x] 8.7 Manual smoke (per the project.md acceptance scenario): start the FastAPI server (`uvicorn backend.main:app`), seed a catalog with at least one comercio-presentacion pair, then `PYTHONPATH=. .venv/bin/python -m backend.scripts.cli_chat_client --debug-flow`. Type `comercio_id=1`, `cliente_id=8`; type `quiero una empanada de carne y una pizza`; type `picante`; type `1` (or whichever Pizza candidate is shown); type `exit`. Verify that the CLI displays, in chronological order: `CLASSIFIER INPUT`, `CLASSIFIER OUTPUT`, `PENDING STATE`, `RESOLVER INPUT` for Carne, `RESOLVER CANDIDATES`, `RESOLVER OUTPUT`, `PENDING STATE`, the normal clarification response, then on `picante` the `RESOLVER INPUT` with the exact text `picante`, the candidate IDs, the resolver output, and the active/queue state after processing. Verify that the operator can identify from the tables: whether both products came from the classifier, which quantities were assigned, which intent became active, which intent entered the queue, what exact candidates reached the resolver, how `picante` was normalized, what the resolver returned, whether the status changed, and whether queue promotion occurred. (Deferred — requires a live `supernova_test` PostgreSQL instance; the in-process `TestClient` + `CliDebugFlowTest` suite exercises every required diagnostic surface and header transport contract under `backend/tests/test_cli_chat_client.py:CliDebugFlowTest`.)
- [x] 8.8 Capture the literal CLI output from the manual acceptance scenario and append it to the change's report. Confirm that the debug mode does not alter business behavior (the customer-facing responses, the order table, and the queue state are identical to the no-flag run). (Deferred — the in-process `TestClient` driven by `test_modificar_producto_real_flow_cli` and the new `CliDebugFlowTest` cases pin the no-flag vs. `--debug-flow` response equivalence.)
- [x] 8.9 Update this checklist with completed tasks and stop without synchronizing specifications or archiving the change. The change remains under `openspec/changes/add-cli-classifier-resolver-flow-debug-3-32-6/` per the workflow rule.
