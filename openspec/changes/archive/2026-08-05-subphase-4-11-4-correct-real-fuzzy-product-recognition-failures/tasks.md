## 1. Roadmap and Inspection

- [x] 1.1 Replace the pending `### Implement Subphase 4.11.4 — Correct Real Fuzzy Product Recognition Failures. [ ]` line in `openspec/specs/project.md` with the proper `### Subphase 4.11.4 — Correct Real Fuzzy Product Recognition Failures [ ]` pending entry (objective, context, scope, out-of-scope, files affected). Leave the implementation-step placeholder lines in the project roadmap so the completion task can replace them with the final report.
- [x] 1.2 Re-read the Subphase 4.11.3 after-fix report at `openspec/changes/archive/2026-08-04-subphase-4-11-3-calibration-evaluation-mismatch/diagnostics/after.json` and the per-case diagnostic at `after.diagnose.json`; record the 29 `real_fuzzy_recognizer_failure` case_ids and the 8 `real_hybrid_recognizer_failure` case_ids, and confirm every fuzzy failure carries `catalog_scope: "commerce_dynamic_database"`. Record the root-cause summary: Subphase 4.11.3 proved the evaluator and expected identifiers are valid (all seven dataset / seed / identifier categories at `0`); it did not prove the runtime fuzzy recognizer fails for the 29 cases — it proved only that the offline calibration runner hands the recognizer an empty catalog at `runner.py:636` while the vector search at `runner.py:654-660` is correctly given `allowed_candidate_ids`.
- [x] 1.3 Inspect the runtime catalog assembly path: `backend/tests/manual_product_recognizer.py::_load_catalog` (line 31), `backend/intents/context/product_selection_context_resolver.py:165`, the matching paths in `backend/intents/context/product_modification_resolver.py`, `backend/intents/recognizers/quitar_producto_recognizer.py`, and `backend/intents/recognizers/modificar_producto_recognizer.py`. Pin the documented runtime field set (`producto_presentacion_id`, `producto_id`, `presentacion_id`, `categoria_id`, `producto_nombre`, `categoria_nombre`, `presentacion_codigo`, `presentacion_descripcion`, `activo`, `producto_activo`, `presentacion_activo`, `disponible`) and the recognizer's `disponibles` / `encontrados_no_disponibles` split at `backend/recognizers/product_recognizer.py:543-561`. Record the exact runtime callsite for each assembly step.

## 2. Per-Commerce Catalog Loader

- [x] 2.1 Add `backend/services/product_recognition_calibration_commerce_catalog.py` exporting a `CommerceCatalog` dataclass with fields `id_comercio: int` and `entries: tuple[dict, ...]` (the entries are the runtime-shaped `producto_presentacion` dicts in `producto_presentacion_id` ascending order). The dataclass SHALL be frozen.
- [x] 2.2 Implement `load_commerce_catalog_from_database(session, id_comercio) -> CommerceCatalog` (the fresh-DB loader; the only runtime source of truth) in `backend/services/product_recognition_calibration_commerce_catalog.py`. The function mirrors `backend/tests/manual_product_recognizer.py::_load_catalog` for the given commerce — joining `ProductoPresentacion` with `Producto` and `Presentacion`, filtering to `id_comercio == id_comercio`, returning the runtime-shaped entries with all four availability flags preserved (`activo`, `producto_activo`, `presentacion_activo`, `disponible`), sorted by `producto_presentacion_id`, deduped by `producto_presentacion_id`. The function SHALL execute exactly one query per call and SHALL NOT issue a second query.
- [x] 2.3 Implement `validate_commerce_catalog_inventory_shape(inventory, id_comercio) -> None` (an evidence-shape validator, NOT a runtime loader). The function reads `dataset["commerce_catalog_inventory"][str(id_comercio)]`, validates the entry shape against the documented runtime field set, sorts / dedupes to confirm determinism, and returns. The function does NOT return the inventory; it only confirms the persisted block is well-formed. This function is used by the inventory regeneration step and by tests; the runner NEVER hands its result to the recognizer.
- [x] 2.4 Implement `fingerprint_commerce_catalog(catalog: CommerceCatalog) -> str` that returns the lowercase-hex SHA-256 of the canonical JSON of `catalog.entries` (sorted object keys, stable list order, finite JSON values). Idempotent: identical inputs produce identical fingerprints.
- [x] 2.5 Add focused unit tests in `backend/tests/test_product_recognition_calibration_commerce_catalog.py` covering: (a) entries sorted by `producto_presentacion_id` ascending; (b) duplicates deduped; (c) fingerprint is deterministic for fixed inputs; (d) fingerprint changes when any field changes; (e) cross-commerce queries return only the requested commerce; (f) the documented runtime field set is required and the loader rejects malformed entries with a deterministic error.

## 3. Inventory Regeneration

- [x] 3.1 Extend `backend/scripts/calibration_inventory.py` with a new `--mode regenerate-commerce-catalog --commerce-id <id>` subcommand (no required change to the existing `--mode regenerate` flow). The new subcommand SHALL load the per-commerce catalog through the fresh-DB loader `load_commerce_catalog_from_database(session, id_comercio)`, validate the shape, persist it under `dataset["commerce_catalog_inventory"][str(id_comercio)]` as reproducible evidence of the database snapshot, compute the fingerprint through `fingerprint_commerce_catalog`, and persist it under `dataset["commerce_catalog_fingerprint"][str(id_comercio)]`. The existing `_resolve_seed_refs`, `_validate_seed_refs`, `_regenerate`, and `--mode regenerate` are unchanged; the existing `seed_refs` map and `inventory_fingerprint` derivation are unchanged.
- [x] 3.2 Update `backend/services/product_recognition_calibration_policy.py::validate_dataset` to accept the optional top-level `commerce_catalog_inventory` and `commerce_catalog_fingerprint` blocks on `schema_version: 3` datasets without rejecting legacy datasets that omit them and without bumping `schema_version`. The existing `seed_refs` validation, `correction_evidence` validation, and `inventory_fingerprint` derivation MUST be unchanged.
- [x] 3.3 Add a focused regression test that mutates `commerce_catalog_inventory` between two regenerations against an unchanged database and asserts the two outputs are byte-identical (idempotent regeneration). Add a focused regression test that mutates one entry's `producto_nombre` and asserts the `commerce_catalog_fingerprint` changes.

## 4. Runner Integration

- [x] 4.1 In `ProductRecognitionCalibrationRunner.run`, replace the empty-catalog branch at `runner.py:636` and `runner.py:677` with a dispatch: when `source_case["catalog_scope"] == "commerce_dynamic_database"`, the runner resolves the per-commerce catalog through a new `self._commerce_catalog_cache: dict[int, CommerceCatalog]` (loaded once per commerce, reused for every case at that commerce); otherwise (the `in_memory` branch), keep the embedded catalog verbatim. The dispatch SHALL NOT consult `allowed_candidate_ids`, `restricted_candidate_ids`, `expected_decision`, `expected_producto_presentacion_id`, `expected_producto_presentacion_id_ref`, or any other expected label. The vector search branch at `runner.py:650-660` is unchanged.
- [x] 4.2 Implement the load → validate → cache flow for every `id_comercio` the runner visits. On first reference to a given `id_comercio`, the runner calls `load_commerce_catalog_from_database(self._session, id_comercio)` exactly once; the returned `CommerceCatalog` is the fresh DB catalog and is the runtime source of truth for that commerce. Immediately after loading, the runner computes the fingerprint through `fingerprint_commerce_catalog(catalog)` and compares it against `dataset["commerce_catalog_fingerprint"][str(id_comercio)]`. On mismatch, or when the persisted `commerce_catalog_fingerprint` block is missing the entry for that `id_comercio`, raise `StaleCommerceCatalogError` naming the offending `id_comercio` and the expected / actual fingerprints (or "missing" for the absent case); the CLI returns non-zero and no report is emitted. On success, the runner caches that SAME `CommerceCatalog` object on `self._commerce_catalog_cache: dict[int, CommerceCatalog]` and reuses it for every subsequent case at that commerce — the catalog handed to the recognizer is the same fresh DB catalog that was just validated, never a reload and never the persisted inventory. The runner MUST NOT read `dataset["commerce_catalog_inventory"][...]` to populate the cache; that block is evidence only and is consulted solely for the fingerprint comparison above. The cache SHALL be populated lazily; the runner SHALL NOT pre-load catalogs for commerces it never visits. The existing `seed_refs` / `inventory_fingerprint` stale guard at `runner.py:612-628` is unchanged and runs first.
- [x] 4.3 Implement the `StaleCommerceCatalogError` exception class in `backend/services/product_recognition_calibration_commerce_catalog.py` (or a sibling module) with the documented fields: `id_comercio`, `expected_fingerprint` (the persisted value, or `None` when the persisted entry is absent), `actual_fingerprint` (the fresh DB fingerprint), and a deterministic message. The CLI handler converts the exception into non-zero exit code and emits no report. The exception class MUST NOT carry any field that depends on the case catalog (`allowed_candidate_ids`, `restricted_candidate_ids`, etc.).
- [x] 4.4 Apply `allowed_candidate_ids` and `restricted_candidate_ids` strictly in the evaluator after recognition. The existing boundary check at `runner.py:654-660` (vector search filtered by `set(case["allowed_candidate_ids"])`) is unchanged; the evaluator additionally flags any candidate returned by the fuzzy recognizer whose id is in `restricted_candidate_ids` (or whose id is not in `allowed_candidate_ids` when the case has a non-empty `allowed_candidate_ids`). The per-case `candidate_boundary_violation` failure category and the Subphase 4.11.3 `mismatch_category` taxonomy are unchanged.
- [x] 4.5 Confirm the runner still imports cleanly (no new module cycles), still fails closed on stale inventory and stale catalog, and still surfaces the Subphase 4.11.3 `mismatch_category_counts` aggregate and per-case `mismatch_category` field. The 11 preserved `in_memory` cases SHALL produce byte-identical observations modulo the documented new fields.

## 5. Inventory Regeneration and Calibration Re-run

- [x] 5.1 Run `python -m backend.scripts.calibration_inventory --mode regenerate --dataset backend/data/product_recognition_calibration_cases.json` to refresh `seed_refs` and `inventory_fingerprint` (unchanged behavior). Then run `python -m backend.scripts.calibration_inventory --mode regenerate-commerce-catalog --commerce-id 1 --dataset backend/data/product_recognition_calibration_cases.json` to add `commerce_catalog_inventory["1"]` and `commerce_catalog_fingerprint["1"]`. Commit the regenerated dataset under the change root.
- [x] 5.2 Run `python -m backend.cli.calibrate_product_recognizer --dataset backend/data/product_recognition_calibration_cases.json --output <change-root>/diagnostics/after.json --diagnose --diagnose-output <change-root>/diagnostics/after.diagnose.json` against the regenerated dataset and capture the new JSON report and per-case diagnostic. Document the exact command in the completion report.
- [x] 5.3 Bucket the new diagnostic into the ten categories and confirm: the empty-catalog defect is eliminated (fuzzy cases that previously returned `unknown` due to empty catalog now return real recognizer output); no expected-case field influenced catalog construction (the catalog is byte-identical across cases at the same commerce); residual failures are reclassified under the Subphase 4.11.3 taxonomy honestly; categories with zero incorrect cases remain at zero (no fabrication); `mismatch_category_counts.total` equals 47 minus the new `correct` count. Do NOT promise that any specific pre-fix count will move to zero.

## 6. Focused Regression Tests

- [x] 6.1 Add `backend/tests/test_product_recognition_calibration_4_11_4.py` with focused tests proving no label leakage and the documented per-commerce caching invariants: (a) the runner builds a catalog for every `commerce_dynamic_database` case and the catalog is the fresh DB catalog — not the persisted `commerce_catalog_inventory[<id_comercio>]`, even when both contain the same entries; (b) the catalog is byte-identical across cases at the same `id_comercio`; (c) changing `allowed_candidate_ids` does NOT change the catalog; (d) changing `expected_producto_presentacion_id` does NOT change the catalog; (e) restricted candidates remain present in the fresh DB catalog and the evaluator flags them when returned; (f) entries from another commerce are absent (commerce isolation); (g) inactive and unavailable entries remain present in the fresh DB catalog with their original four runtime availability flags preserved exactly and the recognizer routes matching unavailable entries into `encontrados_no_disponibles` while available entries still appear in `disponibles`; (h) no inactive entry is silently removed before recognition; (i) catalog construction is deterministic (sorted, deduped, byte-identical re-runs); (j) the fresh DB catalog is loaded exactly once per commerce per calibration run (no per-case database query, no per-call database query); (k) the 11 preserved `in_memory` cases are unchanged (byte-identical observations modulo new fields); (l) no new CLI flag or runtime recognizer behavior is introduced; (m) the `StaleCommerceCatalogError` refusal fails closed with a deterministic error naming the offending `id_comercio` and the expected / actual fingerprints when the fresh DB fingerprint does not match the persisted `commerce_catalog_fingerprint[<id_comercio>]`; (n) the `StaleCommerceCatalogError` refusal also fails closed when the persisted `commerce_catalog_fingerprint` block is missing the entry for the visited `id_comercio`; (o) the existing `seed_refs` / `inventory_fingerprint` stale guard is unchanged and still triggers independently; (p) catalog contents remain independent from expected-case fields.
- [x] 6.2 Add a competing-product regression test that pins a `commerce_dynamic_database` case whose `allowed_candidate_ids = [1, 9, 39]` and whose input text strongly matches a competing `producto_presentacion` (e.g. id `40`) that is NOT in `allowed_candidate_ids`; assert the entry for id `40` is present in the catalog handed to the recognizer and the recognizer MAY return it as a candidate (the recognizer is not artificially constrained). The evaluator then flags id `40` as out-of-allowed, proving the boundary check happens after recognition.
- [x] 6.3 Confirm the Subphase 4.11, 4.11.1, 4.11.2, and 4.11.3 test suites (`backend/tests/test_product_recognition_calibration_runner.py`, `backend/tests/test_product_recognition_calibration_dataset_4_11_1.py`, `backend/tests/test_product_recognition_calibration_eligibility.py`, `backend/tests/test_product_recognition_calibration_report.py`, `backend/tests/test_product_recognition_calibration_policy.py`, `backend/tests/test_product_recognition_calibration_cli.py`, `backend/tests/test_product_recognition_calibration_4_11_3.py`) remain green and the runner-import-graph is still clean of `backend.tests.*`, `backend.scripts.calibration_inventory` (when imported from production code), and any fixture module.

## 7. Verification

- [x] 7.1 Run `PYTHONPATH=. venv/bin/pytest backend/tests/test_product_recognition_calibration_4_11_4.py backend/tests/test_product_recognition_calibration_commerce_catalog.py -vv` and report the exact pass count.
- [x] 7.2 Run the focused Subphase 4.11 / 4.11.1 / 4.11.2 / 4.11.3 test suites and confirm zero regressions.
- [x] 7.3 Run `PYTHONPATH=. venv/bin/python -m ruff check` over every touched Python file and fix only failures introduced by this change.
- [x] 7.4 Run `PYTHONPATH=. venv/bin/python -m mypy --strict` over every touched production file and report any pre-existing unrelated failures separately.
- [x] 7.5 Run `PYTHONPATH=. venv/bin/python -m compileall` over every touched Python file and confirm exit 0.
- [x] 7.6 Run `openspec validate subphase-4-11-4-correct-real-fuzzy-product-recognition-failures --strict` and confirm the active change remains valid, unsynchronized, and unarchived.

## 8. Reporting

- [x] 8.1 Report the corrected root-cause summary: Subphase 4.11.3 proved the evaluator and expected identifiers are valid; the demonstrated defect is the calibration runner passing an empty catalog for `commerce_dynamic_database` cases at `runner.py:636`; Subphase 4.11.4 corrects this calibration-environment defect by loading the full runtime-compatible commerce catalog from PostgreSQL once per commerce at calibration time (every commerce-scoped `producto_presentacion` entry returned by the real runtime catalog assembly, with all four runtime availability flags preserved and inactive entries routed by the recognizer's existing `disponibles` / `encontrados_no_disponibles` split), and by comparing the fresh DB fingerprint against the persisted `commerce_catalog_fingerprint[<id_comercio>]` to detect drift (failing closed with `StaleCommerceCatalogError` on mismatch); only the failures that remain after that correction may be classified as genuine fuzzy recognizer failures requiring production recognizer changes. Do NOT claim the 29 pre-fix failures directly translate to customer-facing runtime failures unless new runtime evidence proves it. Report the new `commerce_catalog_inventory` and `commerce_catalog_fingerprint` blocks (reproducible evidence only — the runner does not hand them to the recognizer); the per-commerce caching strategy; the database query / caching strategy (exactly one fresh DB query per commerce per run, cached on `self._commerce_catalog_cache`); the per-case `mismatch_category` counts before vs after (honestly recorded); the before / after metrics; the final eligibility status with documented reasons; the residual `real_fuzzy_recognizer_failure` / `real_hybrid_recognizer_failure` cases (if any) that survive the calibration-environment correction and remain the documented future work; the exact calibration command; and confirmation that hybrid runtime was not activated and that no recognizer mode, embedding model, or runtime call site changed.
- [x] 8.2 Update `openspec/specs/project.md` to replace the pending `### Subphase 4.11.4 — Correct Real Fuzzy Product Recognition Failures [ ]` entry with the proper `### Subphase 4.11.4 — Correct Real Fuzzy Product Recognition Failures [x] — completed` entry following the exact structure used by Subphase 4.11.3 (objective, context, completed outcomes, architectural constraints, relevant files, OpenSpec, context for future subphases).
- [x] 8.3 Leave the change under `openspec/changes/subphase-4-11-4-correct-real-fuzzy-product-recognition-failures/`; do not run specification sync or archive.
