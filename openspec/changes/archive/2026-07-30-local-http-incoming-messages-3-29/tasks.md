## 1. Request and Response Schemas

- [x] 1.1 Create `backend/schemas/incoming_message.py` with two Pydantic models: `IncomingMessageRequest` (single field `message: str`, `model_config = ConfigDict(extra="forbid")`) and `IncomingMessageResponse` (single field `responses: list[CustomerResponse]`, `model_config = ConfigDict(from_attributes=True)`). Import `CustomerResponse` from `backend.intents.schemas.customer_response`. Declare `__all__ = ["IncomingMessageRequest", "IncomingMessageResponse"]`.
- [x] 1.2 Verify `IncomingMessageRequest(message="hola").message == "hola"`; verify Pydantic rejects `IncomingMessageRequest(message=None)`, `IncomingMessageRequest(message=123)`, and `IncomingMessageRequest(message="hola", extra_field="x")` with `pydantic.ValidationError`.

## 2. Router Implementation

- [x] 2.1 Create `backend/routers/incoming_messages.py` with `from fastapi import APIRouter, Depends, HTTPException, status`, `from sqlalchemy.orm import Session`, the typed-alias import `Session as DatabaseSession` (kept locally for clarity), `from backend.dependencies import get_session`, `from backend.intents.orchestration.incoming_message_response_orchestrator import process_incoming_message_with_responses`, `from backend.schemas.incoming_message import IncomingMessageRequest, IncomingMessageResponse`, `from backend.services.exceptions import SessionNotFound`, and `from backend.services.session_service import SessionService`. Declare `__all__ = ["router"]`.
- [x] 2.2 Create `router = APIRouter(tags=["incoming-messages"])` and a small `_service(session: DatabaseSession = Depends(get_session)) -> SessionService: return SessionService(session)` dependency factory. No additional dependencies, no background tasks, no sub-routers.
- [x] 2.3 Implement `post_incoming_message(comercio_id: int, cliente_id: int, payload: IncomingMessageRequest, service: SessionService = Depends(_service), db: DatabaseSession = Depends(get_session)) -> IncomingMessageResponse`. The handler MUST be decorated with `@router.post("/comercios/{comercio_id}/clientes/{cliente_id}/incoming-messages", response_model=IncomingMessageResponse, status_code=status.HTTP_200_OK)` and MUST be the only route decorator in the module.
- [x] 2.4 In the handler, resolve the active session with `session = service.get_active(comercio_id, cliente_id)`; on `SessionNotFound as exc` raise `HTTPException(status_code=404, detail=str(exc)) from exc`. Do NOT call `db.commit` / `db.rollback` / `db.flush` / `db.refresh` / `db.expire` / `db.begin` anywhere in the handler.
- [x] 2.5 Call `responses = process_incoming_message_with_responses(db, session, payload.message)` exactly once. On `TypeError as exc` raise `HTTPException(status_code=400, detail=str(exc)) from exc`; on `ValueError as exc` raise `HTTPException(status_code=400, detail=str(exc)) from exc`. Any other exception propagates unchanged (no `except Exception`, no wrapping, no swallowing).
- [x] 2.6 Return `IncomingMessageResponse(responses=responses)`; do NOT add `session_id`, `comercio_id`, `cliente_id`, `timestamp`, or any other field; do NOT transform the customer responses; do NOT reorder the list.

## 3. App Wiring

- [x] 3.1 Edit `backend/main.py` to add `incoming_messages` to the existing `from backend.routers import (...)` tuple (alphabetical order is fine but not required) and append `app.include_router(incoming_messages.router)` next to the existing `app.include_router(sessions.router)` line.
- [x] 3.2 Confirm `backend/main.py` still constructs the `FastAPI(title="supernova-ia API")` app, still includes every existing router, and still imports no additional symbols.

## 4. Endpoint Tests

- [x] 4.1 Create `backend/tests/test_incoming_messages_endpoint.py` with module-level imports: `unittest`, `unittest.mock.MagicMock` / `patch`, `pydantic` (only for the ValidationError assertion), `fastapi.FastAPI`, `fastapi.testclient.TestClient`, `backend.routers.incoming_messages as router_module`, and `backend.intents.orchestration.incoming_message_response_orchestrator.process_incoming_message_with_responses` (kept as a stable handle). Build a fresh `FastAPI()` app instance, include the router, override `backend.dependencies.get_session` with a `MagicMock(name="DatabaseSession")` factory, and expose a module-level `client = TestClient(app)`.
- [x] 4.2 Happy-path test (`agregar_producto` executed): patch `router_module.SessionService.get_active` to return a `MagicMock(name="ConversationSession")` and `router_module.process_incoming_message_with_responses` to return `[CustomerResponse(message="Listo, agregué 2 Pizza Mozzarella grande.", intent="agregar_producto", status="executed")]`. POST `/comercios/1/clientes/2/incoming-messages` with body `{"message": "quiero una pizza"}`. Assert HTTP 200, JSON body `{"responses": [{"message": "Listo, agregué 2 Pizza Mozzarella grande.", "intent": "agregar_producto", "status": "executed"}]}`, `SessionService.get_active.assert_called_once_with(1, 2)`, `process_incoming_message_with_responses.assert_called_once_with(<db>, <session>, "quiero una pizza")`.
- [x] 4.3 Happy-path test (`desconocida` unsupported): same as 4.2 but with `process_incoming_message_with_responses` returning `[CustomerResponse(message=GENERIC_MESSAGE, intent="desconocida", status="rejected")]`. Assert HTTP 200, JSON body matches, and the `GENERIC_MESSAGE` constant is the one from `backend.intents.orchestration.incoming_message_response_orchestrator`.
- [x] 4.4 Multi-intent order test: `process_incoming_message_with_responses` returns three customer responses in the order `[agregar_producto executed, agregar_producto pending_resolution, desconocida rejected]`. Assert HTTP 200 and JSON `responses` preserves that order.
- [x] 4.5 Empty list test: `process_incoming_message_with_responses` returns `[]`. Assert HTTP 200 and JSON body is `{"responses": []}`.
- [x] 4.6 `SessionNotFound` → 404 test: patch `router_module.SessionService.get_active` to raise `SessionNotFound((1, 2))`. POST the endpoint; assert HTTP 404, JSON body `{"detail": <str(exc)>}`, and `process_incoming_message_with_responses.assert_not_called()`.
- [x] 4.7 `TypeError` → 400 test: patch `router_module.SessionService.get_active` to return a session and `router_module.process_incoming_message_with_responses` to raise `TypeError("message must be a str")`. POST `{"message": None}` (bypassing Pydantic by sending raw JSON via `client.post(..., json={"message": None})`); assert HTTP 400, JSON body `{"detail": "message must be a str"}`. (Confirm Pydantic actually accepts `null` for `message` because `str` allows it; if Pydantic rejects it, switch the test to `TypeError` raised by the inner pipeline via a separate `{"message": ""}` request.)
- [x] 4.8 `ValueError` → 400 test (empty message): patch `router_module.process_incoming_message_with_responses` to raise `ValueError("message must be a non-empty, non-whitespace string")`. POST `{"message": ""}`; assert HTTP 400, JSON body `{"detail": "message must be a non-empty, non-whitespace string"}`.
- [x] 4.9 `ValueError` → 400 test (whitespace message): patch the inner call to raise `ValueError(...)`. POST `{"message": "   \n\t  "}`; assert HTTP 400 and the JSON `detail` matches the exception message.
- [x] 4.10 Pydantic 422 tests: assert HTTP 422 for `{"message": 123}`, `{}` (missing field), and `{"message": "hola", "extra_field": "x"}`. The handler MUST NOT be invoked in any of these cases (verify via `SessionService.get_active.assert_not_called()` and `process_incoming_message_with_responses.assert_not_called()`).
- [x] 4.11 Unhandled exception propagation test: patch `router_module.process_incoming_message_with_responses` to raise `RuntimeError("boom")`. POST a valid request; assert HTTP 500 (FastAPI's default handler) and the JSON `detail` does NOT contain the seam's own wrapping (i.e., it surfaces the original exception class or the FastAPI default message, not a custom seam-level translation).
- [x] 4.12 Router module `__all__` discipline: import `backend.routers.incoming_messages`; assert `module.__all__ == ["router"]`.
- [x] 4.13 Schemas module `__all__` discipline: import `backend.schemas.incoming_message`; assert `module.__all__ == ["IncomingMessageRequest", "IncomingMessageResponse"]`.
- [x] 4.14 Router source-code boundary test: read `backend/routers/incoming_messages.py` from disk; assert the source does NOT contain `"from backend.old_project"`, `"from backend.llm"`, `"from backend.repositories"`, `"from backend.intents.handlers"`, `"from backend.intents.context"`, `"from backend.intents.recognizers"`, `"from backend.intents.resolvers"`, `"from backend.intents.processor"`, `"from backend.intents.contracts"`, `"from backend.intents.orchestration"`, `"import requests"`, `"import twilio"`, `"MessagingResponse"`, `"asyncio"`, `"async def"`, `"await "`, `"logger."`, `"logging."`, `"print("`, `"time.sleep"`, `"retry"`, `"backoff"`, `"db.commit"`, `"db.rollback"`, `"db.flush"`, `"db.refresh"`, `"db.expire"`, or `"db.begin"`. (Exception: `process_incoming_message_with_responses` is the only allowed `backend.intents.orchestration` reference; the assertion only flags the string when it appears inside an import statement or a function body outside the allowed list — adjust the substring list to be conservative if necessary.)
- [x] 4.15 Single-route-decorator discipline: read the router module source from disk; assert it contains exactly one `@router.post(...)` decorator and zero `@router.get`, `@router.put`, `@router.patch`, or `@router.delete` decorators.
- [x] 4.16 Add `if __name__ == "__main__": unittest.main(verbosity=2)` at the bottom of the test module so the runner pattern matches the other intent tests.

## 5. Spec Sync

- [x] 5.1 Append the new `## ADDED Requirements` block from `openspec/changes/local-http-incoming-messages-3-29/specs/incoming-messages-local-http-endpoint/spec.md` into the live `openspec/specs/incoming-messages-local-http-endpoint/spec.md`. The live spec is brand new for this capability; do not modify any existing requirements in other capabilities.
- [x] 5.2 Append the new `## ADDED Requirements` block from `openspec/changes/local-http-incoming-messages-3-29/specs/incoming-message-response-orchestrator/spec.md` into the live `openspec/specs/incoming-message-response-orchestrator/spec.md`. Do not modify the existing requirements in the live spec; the addition only documents that a local HTTP seam now exists.

## 6. Verification

- [x] 6.1 Run `PYTHONPATH=. venv/bin/python backend/tests/test_incoming_messages_endpoint.py` and confirm every assertion passes without a real LLM call and without a real `supernova_test` connection.
- [x] 6.2 Run `PYTHONPATH=. venv/bin/python -m unittest backend.tests.test_incoming_messages_endpoint` and confirm a clean pass.
- [x] 6.3 Run `PYTHONPATH=. venv/bin/python -m unittest backend.tests.test_incoming_message_response_orchestrator backend.tests.test_transactional_message_processor backend.tests.test_incoming_message_orchestrator backend.tests.test_initial_intent_dispatcher` and confirm the existing suites still pass.
- [x] 6.4 Run `PYTHONPATH=. venv/bin/python -m compileall backend` and confirm exit 0.
- [x] 6.5 Use `fastapi.testclient.TestClient` against the final `backend.main.app`; confirm `GET /openapi.json` documents `POST /comercios/{comercio_id}/clientes/{cliente_id}/incoming-messages` and a POST to `/comercios/1/clientes/2/incoming-messages` with an empty JSON body returns HTTP 422 rather than 404 or 405, proving the registered route is reachable without relying on FastAPI's deferred `_IncludeRouter` internals.
- [x] 6.6 Run `openspec validate local-http-incoming-messages-3-29 --strict` and confirm valid.
- [x] 6.7 Perform a non-destructive final verification that `backend/routers/incoming_messages.py`, `backend/schemas/incoming_message.py`, and `backend/tests/test_incoming_messages_endpoint.py` remain present and `backend/main.py` still includes `incoming_messages.router`; do not roll back, remove, or disable the endpoint required by the proposal, design, and specs.