## 1. Backend schema and repository

- [x] 1.1 Add `PedidoDetalleLinea` and `PedidoDetalleResponse` to `backend/schemas/pedido.py`. `PedidoDetalleLinea` carries `cantidad: int`, `producto_nombre: str`, `presentacion_descripcion: str`, declares `model_config = ConfigDict(extra="forbid", from_attributes=True)`. `PedidoDetalleResponse` carries the same scalar fields as `PedidoResponse` (`id`, `id_session`, `id_medio_pago`, `id_metodo_entrega`, `datetime_entrega_programada`, `estado_pedido`, `fecha_alta`, `fecha_ultima_modificacion`) plus `lineas: list[PedidoDetalleLinea]`, declares `model_config = ConfigDict(extra="forbid")`. Add both names to a module-level `__all__` if not already exported.
- [x] 1.2 Extend `PedidoProductoRepository.list_by_pedido` in `backend/repositories/pedido_producto_repository.py` to add `joinedload(ProductoPresentacion.presentacion)` to the existing options chain. Keep the existing order by `id`. No new method.
- [x] 1.3 Confirm with `PYTHONPATH=. venv/bin/python -c "from backend.schemas.pedido import PedidoDetalleResponse, PedidoDetalleLinea; print(PedidoDetalleResponse.model_fields.keys())"` that the new schemas import and expose the expected fields.

## 2. Backend service and router

- [x] 2.1 Add `get_detalle(pedido_id: int) -> tuple[Pedido, list[PedidoProducto]]` to `PedidoService` in `backend/services/pedido_service.py`. Inject the existing `PedidoProductoRepository` (it is constructed in `__init__` alongside the pedido repository). Load the `Pedido` via `self._repo.get(pedido_id)`, raise `PedidoNotFound(pedido_id)` when missing. Load the line items via `self._pedido_producto_repo.list_by_pedido(pedido_id)`. Do NOT call `commit`, `rollback`, `flush`, `refresh`, `expire`, or `begin`. Return the `(pedido, lineas)` pair.
- [x] 2.2 Add `get_pedido_detalle(pedido_id, service = Depends(_service))` to `backend/routers/pedidos.py`. The handler calls `pedido, lineas = service.get_detalle(pedido_id)`, builds `PedidoDetalleLinea` for each row (substituting `—` for missing or empty `presentacion.descripcion`), and returns `PedidoDetalleResponse` with the pedido scalars and the built `lineas` list. Translate `PedidoNotFound` to `HTTPException(404)`. Declare `response_model=PedidoDetalleResponse`. No new router module, no new tag, no change to `backend/main.py`.
- [x] 2.3 Confirm with `PYTHONPATH=. venv/bin/python -c "from backend.routers.pedidos import router; routes = [r.path for r in router.routes if hasattr(r, 'path')]; print([r for r in routes if 'detalle' in r])"` that the new route is registered under the `/pedidos` prefix and only `GET` is exposed for it.

## 3. CLI pure helpers

- [x] 3.1 Add the module-level constant `ORDER_MUTATING_INTENTS = {"agregar_producto", "quitar_producto"}` to `backend/scripts/cli_chat_client.py`. Add a comment referencing subphase 3.30.2 so future subphases extend this set in one place.
- [x] 3.2 Add `response_modified_order(responses) -> bool` to `backend/scripts/cli_chat_client.py`. The function iterates `responses`, ignores non-`dict` elements, and returns `True` iff at least one element carries `status == "executed"` and `intent in ORDER_MUTATING_INTENTS`. Use `argparse`-free Python (no new imports).
- [x] 3.3 Add `format_order_table(lineas) -> str` to `backend/scripts/cli_chat_client.py`. When `lineas` is empty, return `"Pedido actual: vacío\n"`. Otherwise: compute dynamic column widths from the headers (`Producto`, `Presentación`, `Cantidad`) and the values, substitute `—` (U+2014) for missing or empty `presentacion_descripcion`, render a header strip, one row per line item, and a closing strip. Use only `str` operations and f-strings. End the output with a single `\n`. Do not import any module.
- [x] 3.4 Add the names to `__all__`. The list now reads: `["main", "_post_json", "_read_int", "_print_responses", "_resolve_base_url", "_close_session", "_create_session", "_get_active_session", "_fetch_pedido_detalle", "response_modified_order", "format_order_table", "ORDER_MUTATING_INTENTS"]`.

## 4. CLI integration

- [x] 4.1 Add `_fetch_pedido_detalle(base_url: str, pedido_id: int) -> tuple[int, object] | tuple[str, str]` to `backend/scripts/cli_chat_client.py`. The function calls `_post_json(f"{base_url}/pedidos/{pedido_id}/detalle", {}, method="GET")`. On `status == 200` and a `dict` payload, return `(200, payload)`. On non-200, return `("warning", f"HTTP {status}: {detail}")`. On raised exception, return `("warning", str(exc))`. Never raises.
- [x] 4.2 Modify `main()` in `backend/scripts/cli_chat_client.py`. After the existing `_print_responses(payload)` call inside the `try` block, extract `responses = payload.get("responses", [])` and, if `response_modified_order(responses)` is `True`, call `_fetch_pedido_detalle(base_url, pedido_id)`. On `(200, dict_payload)`, build `lineas = payload.get("lineas", [])` (defaulting to `[]` when missing or not a list) and print `format_order_table(lineas)`. On `("warning", msg)`, print `Warning: the order was modified, but its updated detail could not be retrieved. ({msg})` and continue. On any unexpected return shape, do nothing (the loop continues). The `try / finally` cleanup of the session, the `exit` handling, the empty-input skipping, and the bootstrap remain unchanged.
- [x] 4.3 Confirm with `PYTHONPATH=. venv/bin/python -c "import backend.scripts.cli_chat_client as cli; print('order mutating:', sorted(cli.ORDER_MUTATING_INTENTS)); print('table empty:', repr(cli.format_order_table([]))); print('table one row:', cli.format_order_table([{'cantidad': 2, 'producto_nombre': 'Pizza de Muzzarella', 'presentacion_descripcion': 'Grande'}]))"` that the helpers produce the expected output.

## 5. Tests

- [x] 5.1 Add a `CliPedidoTableTest` class to `backend/tests/test_cli_chat_client.py`. Cover: (a) executed `agregar_producto` triggers exactly one `GET /pedidos/{pedido_id}/detalle`; (b) executed `quitar_producto` triggers exactly one `GET /pedidos/{pedido_id}/detalle`; (c) `pending_resolution`, `rejected`, `failed`, and conversational (`saludo`) responses trigger zero detail retrievals; (d) a response list with multiple executed mutations triggers exactly one detail retrieval; (e) the customer responses are printed before the table; (f) the table contains `Producto`, `Presentación`, and `Cantidad` columns; (g) the table does not expose any `id*` value (use the `_post_json` body inspection to assert no `id_*` key in the response that becomes the table). Mock `urllib.request.urlopen` via the existing `FakeResponse` helper; patch `builtins.input` for the typed lines.
- [x] 5.2 Add focused unit tests for the pure helpers in `backend/tests/test_cli_chat_client.py`: `test_response_modified_order_true_for_executed_mutation`, `test_response_modified_order_false_for_rejected`, `test_response_modified_order_false_for_pending_resolution`, `test_response_modified_order_false_for_failed`, `test_response_modified_order_false_for_conversational`, `test_response_modified_order_false_for_unknown_intent`, `test_response_modified_order_false_for_empty_responses`, `test_response_modified_order_false_for_non_dict_responses`, `test_format_order_table_empty_returns_vacio`, `test_format_order_table_single_row_contains_columns_and_values`, `test_format_order_table_adapts_to_long_names`, `test_format_order_table_falls_back_to_em_dash_for_missing_presentation`, `test_format_order_table_renders_integer_quantity`. No HTTP mocking required for these.
- [x] 5.3 Add a `test_detail_retrieval_failure_prints_warning_and_continues_loop` test. Drive the CLI with a successful `agregar_producto` response followed by a `GET /pedidos/{pedido_id}/detalle` that returns 500. Assert the customer response is printed, the warning line is printed once, the loop accepts the next typed line, and `exit` still closes the session and exits 0.
- [x] 5.4 Update `test_import_boundary` in `backend/tests/test_cli_chat_client.py` to assert the new helpers (`response_modified_order`, `format_order_table`, `_fetch_pedido_detalle`) do not introduce banned modules. Reuse the existing `vars(cli).values()` walk and the source-grep for the banned identifiers.

## 6. Verification

- [x] 6.1 Run `PYTHONPATH=. venv/bin/python -m compileall backend` from the project root. Expect exit 0.
- [x] 6.2 Run `PYTHONPATH=. ./venv/bin/python -m unittest backend.tests.test_cli_chat_client -v`. Expect all existing tests plus the new ones in step 5 to pass.
- [x] 6.3 Run `PYTHONPATH=. ./venv/bin/python -m unittest backend.tests.api_smoke -v` (or the project's full smoke runner) to confirm no regression in the existing 400+ tests.
- [x] 6.4 Run `openspec validate interactive-cli-pedido-table --strict`. Expect valid.
- [ ] 6.5 Manual smoke (per the project.md acceptance scenario): start the FastAPI server (`uvicorn backend.main:app`), seed a catalog with at least one producto-presentacion pair that has a non-empty `descripcion`, then `PYTHONPATH=. ./venv/bin/python -m backend.scripts.cli_chat_client`. Type `comercio_id=1`, `cliente_id=8`; type `quiero 2 pizzas de muzzarella`; type `Pizza de Muzzarella Grande`; type `quitame 1 pizza`; type `exit`. Verify: bootstrap prints `<session N>` and `<pedido M>`; each executed mutation prints the customer response and a `Pedido actual:` table with one row that shows `Cantidad = 2` after the add and `Cantidad = 1` after the partial quit; the `exit` line closes the session and exits 0; no new imports appear in the CLI; no DB write is performed by the detail read.
- [x] 6.6 Sync the delta specs to `openspec/specs/incoming-messages-interactive-cli/spec.md` and `openspec/specs/pedido-api/spec.md` (append the new `ADDED Requirements` blocks from `openspec/changes/interactive-cli-pedido-table/specs/`). Update the first-line "Purpose" / "TBD" placeholders in those synced specs as needed.
- [x] 6.7 Archive the change: `openspec archive interactive-cli-pedido-table --yes`. Confirm the archive directory under `openspec/changes/archive/2026-07-31-interactive-cli-pedido-table/` contains `proposal.md`, `design.md`, `tasks.md`, and the two `specs/` subdirectories.
- [x] 6.8 Condense the subphase 3.30.2 entry in `openspec/specs/project.md` per the project's "Phase and Subphase Context Rule": replace the detailed subphase body with a concise summary capturing only the new endpoint URL, the schema shape, the CLI trigger condition, the table format, the new files / modified files, the verification commands, and the constraints that future subphases must preserve.
