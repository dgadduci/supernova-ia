## 1. Resolver module

- [x] 1.1 Create `backend/intents/context/pending_product_ambiguity_resolver.py` with `__all__ = ["resolve_pending_product_ambiguity"]`, typed `DatabaseSession = Session` alias (unused locally), and a private `_normalize_tokens` helper that strips diacritics, lowercases, and splits on whitespace.
- [x] 1.2 Implement the input-validation early return: when `active_intent.status != "pending_resolution"` or `active_intent.candidate_ids` is empty, return `active_intent` unchanged (same instance, `is` comparison).
- [x] 1.3 Build a candidate lookup map keyed by `producto_presentacion_id` restricted to `active_intent.candidate_ids`; rows outside this set are ignored even when present in the input list. Each entry exposes `producto_nombre`, `presentacion_descripcion`, `presentacion_codigo`, and the joined normalized tokens.
- [x] 1.4 Implement Layer 1 (numeric) detector returning the selected index when the normalized message contains exactly one in-range Arabic numeral in the accepted shapes (`"1"`, `"2"`, `"la 1"`, `"opción 2"`, `"número 1"`); build the resolved intent via a private `_build_resolved_unique_intent(active_intent, candidate_id)` helper.
- [x] 1.5 Implement Layer 2 (positional) detector returning the selected index for Spanish ordinal tokens (`primera/primero/segunda/segundo/tercera/tercero/última/último`) and the `la opción <n>` / `la opción <word>` shapes; reuse the same resolved-intent helper.
- [x] 1.6 Implement Layer 3 (exact normalized full-name) detector comparing the normalized message tokens against each candidate's normalized full name; reuse the same resolved-intent helper.
- [x] 1.7 Implement Layer 4 (exact token-set after filler-token stripping, with **mandatory shared-core precondition guard**, subset preference, distinguishing-token penalty, and token-count-agnostic ranking). Define a module-private `FILLER_TOKENS` set as a module-level constant. `FILLER_TOKENS` **MUST include `en`** and MAY include other common Spanish prepositions/articles. Strip `FILLER_TOKENS` from both `message_tokens` and each candidate's normalized full-name token set to obtain `message_core` and `candidate_core`. **Guard**: a candidate is eligible for Layer 4 ranking only when `candidate_core ∩ message_core` is non-empty; candidates with zero shared core tokens SHALL be excluded; if no candidate has a non-empty shared core, the layer falls through without selecting. Apply the strict priority to the eligible candidates only: (a) if exactly one eligible candidate has `candidate_core == message_core`, select it; (b) otherwise rank eligible candidates preferring `candidate_core ⊆ message_core`, then smallest `len(candidate_core - message_core)`, then smallest `len(message_core - candidate_core)`; **total candidate token count `len(candidate_core)` is NOT a ranking criterion** — if the top rank is unique only because of token-count differences (with (i)/(ii)/(iii) all tied), fall through (token-count-only unique rank → ambiguous); if the top rank is unique on the documented criteria, select it; otherwise fall through. If `message_core` is empty, fall through. Reuse the same resolved-intent helper.
- [x] 1.8 Implement Layer 5 (differentiating token) detector finding tokens present in exactly one candidate's normalized `producto_nombre`; reuse the same resolved-intent helper.
- [x] 1.9 Implement Layer 6 (contextual default descriptor) detector recognising `común`, `normal`, `regular`, `original`, `clásica`, `clásico`, `estándar` (with diacritic-stripped variants); select the variant-free candidate when one exists; reuse the same resolved-intent helper.
- [x] 1.10 Implement Layer 7 (explicit exclusion) detector recognising `la que no es <token>`, `la que no tenga <token>`, `la que no tiene <token>`, `sin <token>`, `no quiero la <token>`, `no la <token>`, `la otra`; select the candidate whose normalized `producto_nombre` does NOT contain the excluded token; reuse the same resolved-intent helper.
- [x] 1.11 Implement Layer 8 (restricted fuzzy fallback) using `rapidfuzz.fuzz.partial_ratio` with threshold `85`; only score candidates whose id is in `active_intent.candidate_ids`; select the strict highest score when exactly one candidate clears the threshold; reuse the same resolved-intent helper.
- [x] 1.12 Implement Layer 9 (remain ambiguous): when no earlier layer selects, return `active_intent` unchanged (same instance, `is` comparison).
- [x] 1.13 Enforce strict evaluation order via a sequential `for` over a list of layer callables; the first layer that returns a non-`None` selection wins and the loop terminates. No later layer runs once an earlier layer has selected.

## 2. Orchestration integration

- [x] 2.1 Modify `backend/intents/context/product_selection_context_service.py` to load the restricted catalog exactly once via `ProductoQueryService.list_presentaciones_by_ids(active_intent.candidate_ids)`, call `resolve_product_selection(active_intent, catalog)` first, and bind the existing path's result to `fragment_result`. When `fragment_result.status == "ready"`, return `fragment_result` directly without consulting the new resolver.
- [x] 2.2 When `fragment_result.status != "ready"`, the orchestration service MUST invoke `resolve_pending_product_ambiguity` with `fragment_result` (NOT the original `active_intent`) as the active-intent argument and with the in-memory catalog projection filtered to `fragment_result.candidate_ids`. The orchestration service MUST NOT issue a second `list_presentaciones_by_ids` call for the new resolver, MUST NOT widen the catalog beyond `fragment_result.candidate_ids`, and MUST NOT allow the new resolver to reintroduce a candidate that the existing narrowing path discarded. When the new resolver returns a `ready` intent, return it. When the new resolver does NOT resolve (returns `fragment_result` unchanged or further narrowed), return `fragment_result` (the existing path's narrowed candidate list is preserved).
- [x] 2.3 Confirm no commit, rollback, flush, refresh, expire, or `begin` is added; the orchestration service still has no SQLAlchemy queries (the in-memory filter to `fragment_result.candidate_ids` is pure list comprehension, not a SQLAlchemy round-trip) and still does not mutate the `Session` model.

## 3. Focused pytest module

- [x] 3.1 Create `backend/tests/test_pending_product_ambiguity_resolution.py` with module-level `__all__` (only `test_*` functions) and shared helpers that build two-candidate `ProcessedIntent` fixtures for the Coca-Cola Common Lata vs Coca-Cola Zero Lata pair and the Pizza Muzzarella Tradicional vs Pizza Muzzarella Especial pair (generic family).
- [x] 3.2 Layer 1 test: `1` selects Common Lata, `2` selects Zero Lata, `3` falls through, `la 1` selects Common Lata, `opción 2` selects Zero Lata, `número 1` selects Common Lata.
- [x] 3.3 Layer 2 test: `primera` selects Common Lata, `segunda` selects Zero Lata, `la opción dos` selects Zero Lata, `la primera` selects Common Lata, out-of-range ordinals fall through.
- [x] 3.4 Layer 3 test: `coca cola zero lata` selects Zero Lata; mixed case + hyphen (`Coca-Cola Zero Lata`) selects Zero Lata; ambiguous exact-match across candidates falls through.
- [x] 3.5 Layer 4 test: `coca cola en lata` selects Common Lata over Zero Lata; tied scores fall through; intersection-empty falls through; **shared-core precondition** — `banana split` against the Coca-Cola pair has zero shared core with both candidates and remains unresolved at this layer (does not select, does not over-trigger on partial overlap); **token-count-only unique rank** — two eligible candidates that tie on rule (i)/(ii)/(iii) but differ only in `len(candidate_core)` MUST fall through (the layer does NOT rank by token count); guard-correctness assertion — for any candidate where `len(candidate_core ∩ message_core) == 0`, the layer excludes that candidate before any ranking step.
- [x] 3.6 Layer 5 test: `zero` selects Zero Lata; `coca zero` selects Zero Lata; token shared by all candidates falls through.
- [x] 3.7 Layer 6 test: `común` (and `normal`, `regular`, `original`) selects Common Lata; descriptor falls through when every candidate carries a distinguishing variant (e.g. `Zero` + `Light`).
- [x] 3.8 Layer 7 test: `la que no es zero`, `sin zero`, `no quiero la zero`, `la otra` select Common Lata; `la que no es manzana` falls through.
- [x] 3.9 Layer 8 test: a deliberately misspelled message that partial-ratio scores above 85 against exactly one candidate selects that candidate; tied scores fall through; threshold gating verified by a low-similarity message that falls through.
- [x] 3.10 Layer 9 test: `no sé`, `ok`, `quiero hablar con un humano` return the input intent unchanged (same instance, `is` comparison).
- [x] 3.11 Order test: `1` selects via Layer 1 even though Layer 5 could also match (`zero`); strict ordering is preserved.
- [x] 3.12 Second generic family test: `Pizza Muzzarella Tradicional` vs `Pizza Muzzarella Especial` resolved by `1`, `primera`, `tradicional`, `la tradicional`, `la que no es especial`, plus a fuzzy misspelling that still selects Tradicional; ensures no product-specific ID conditionals.
- [x] 3.13 No-side-effects test: input `active_intent` instance is not mutated (`is` comparison); no `commit`, `flush`, `close`, `begin`, `refresh`, `expire` calls; no SQLAlchemy query issued by the resolver.
- [x] 3.14 Public-surface test: `__all__ == ["resolve_pending_product_ambiguity"]`; the file set under `backend/intents/context/` adds only `pending_product_ambiguity_resolver.py`.
- [x] 3.15 **Orchestration discarded-candidate invariant test** — drive `ProductSelectionContextService.resolve` with a fixture that mirrors what `resolve_product_selection` returns when it narrows an `active_intent.candidate_ids == [A, B, C]` down to a `fragment_result` with `candidate_ids == [B, C]` (and a corresponding catalog projection containing only rows for `B` and `C`). Verify that the orchestration service passes `fragment_result` (not the original `active_intent`) to `resolve_pending_product_ambiguity`, that the catalog projection passed to the new resolver is restricted to `[B, C]` (no row for `A`), that the new resolver cannot select `A` even when the customer reply would otherwise uniquely identify `A` (e.g. a differentiating token carried only by `A`), and that when the new resolver does not resolve the orchestration service returns `fragment_result` with `candidate_ids == [B, C]` preserved. The test MUST also assert that `ProductoQueryService.list_presentaciones_by_ids` is called exactly once (via spy/mock or query-counter instrumentation).

## 4. End-to-end conversation integration test

- [x] 4.1 Add `test_coca_cola_common_vs_zero_full_conversation` to `backend/tests/test_pending_product_ambiguity_resolution.py` (or to a sibling file in `backend/tests/`) that seeds `Coca-Cola Común Lata` and `Coca-Cola Zero Lata` against `supernova_test`, runs `dispatch_pending_context(db, session, "1")` (and the alternate replies from Layer 1–7), asserts `status == "executed"`, asserts exactly one `PedidoProducto` row for the Common Lata presentation, asserts `session.pending_intents` is empty, and asserts `session.context_type is None`.
- [x] 4.2 Add `test_coca_cola_zero_selection_via_differentiating_token` that asserts the Zero Lata path (`"zero"`, `"2"`, `"segunda"`, `"coca zero"`, `"la zero"`) selects Zero Lata end-to-end and creates exactly one `PedidoProducto` row for the Zero Lata presentation.
- [x] 4.3 Add `test_second_generic_family_pizza_tradicional_vs_especial` that seeds the Pizza Muzzarella Tradicional vs Pizza Muzzarella Especial pair and runs the same end-to-end scenario for `tradicional` / `la tradicional` / `1` / `la que no es especial` to prove the resolver is generic.
- [x] 4.4 Add `test_vague_answer_remains_ambiguous_through_dispatch` that seeds the Coca-Cola pair and runs `dispatch_pending_context(db, session, "no sé")`; asserts `status == "pending_resolution"`, `session.context_type == "product_selection"` preserved, `candidate_ids` preserved, no `PedidoProducto` row exists.

## 5. Existing behaviour preservation

- [x] 5.1 Run the focused pytest module: `PYTHONPATH=. venv/bin/python -m pytest backend/tests/test_pending_product_ambiguity_resolution.py -vv`. All new tests pass.
- [x] 5.2 Run the listed existing pending-context and end-to-end suites unchanged: `PYTHONPATH=. venv/bin/python -m pytest backend/tests/test_product_selection_context_resolver.py backend/tests/test_pending_context_dispatcher.py backend/tests/test_pending_context_execution.py backend/tests/test_agregar_producto_end_to_end.py backend/tests/test_incoming_message_integration.py backend/tests/api_smoke.py -q`. This subphase **MUST NOT introduce any new regressions** caused by this change. The four `api_smoke.py` tests that already fail on `main` today — `test_llm_settings_and_query_llm`, `test_pending_context_execution`, `test_pending_context_dispatcher`, `test_agregar_producto_end_to_end` — are recorded as **known pre-existing failures** and **MUST NOT be fixed by this subphase**. They MUST be listed separately in the change's implementation log / PR description as out-of-scope and left exactly as they are on `main`.
- [x] 5.3 Run ruff on the touched files: `PYTHONPATH=. venv/bin/python -m ruff check backend/intents/context/pending_product_ambiguity_resolver.py backend/intents/context/product_selection_context_service.py backend/tests/test_pending_product_ambiguity_resolution.py`. Zero findings.
- [x] 5.4 Run compileall on the touched files: `PYTHONPATH=. venv/bin/python -m compileall backend/intents/context/pending_product_ambiguity_resolver.py backend/intents/context/product_selection_context_service.py backend/tests/test_pending_product_ambiguity_resolution.py`. Exit code `0`.

## 6. Validation

- [x] 6.1 Run `openspec validate subphase-4-12a-pending-product-ambiguity-resolution --strict`. Change must validate clean before implementation; re-run after every task that touches the spec files.
- [x] 6.2 Confirm the change remains under `openspec/changes/` (no sync, no archive).
